# CAN 上位机与工具使用

本文同时说明 BMS 上位机、台架模拟从控和 IVT 配置三个界面的职责、F405 接口边界、连接方法、判断结果和现场验证。CAN 帧的 ID、DLC、字段、周期和 F405 安全限制以 [`CAN通信协议.md`](CAN通信协议.md) 为准；IVT 目标配置的完整表格以 [`IVT能量计配置与CANB切换.md`](IVT能量计配置与CANB切换.md) 为准。

上位机安装、源码运行和 Windows 发布见 [`TOOLS/bms_host/README.md`](../TOOLS/bms_host/README.md)。上位机进度、风险、验证和已完成变更只维护在 [`TOOLS/bms_host/todo.md`](../TOOLS/bms_host/todo.md)。

## 1. 工具职责和连接关系

### BMS 上位机

BMS 上位机用于读取 F405 当前状态、查看单体和温度、查看故障、发送 F405 工具命令以及记录/回放 CAN 数据。

底部的“连接上位机”只连接主监视通道，不选择台架或 IVT 配置方式。主监视连接和两个工程工具各自维护连接状态，进入一个工具页不会把主监视切换成另一种模式。

### 台架模拟从控

台架模拟从控用于给 F405 的 CAN1 提供六个从控的测试报文，验证接收、完整性判断、告警和从控离线处理。

它复用 `TOOLS/pcan_bms_bench.py` 的模型和命令。页面只负责连接、发送和显示状态，不改变 F405 Flash，也不伪造 IVT、充电机或 IMD 数据。

### IVT 配置

IVT 配置用于首次安装或维护真实 IVT-S。它在 IVT 所在 CANB 上读取配置，判断当前设备是否已经与 F405 目标一致，并在需要时完成一次性配置或切换 250/500 kbit/s 位率。

两个工程工具默认收在左侧“工程工具”折叠项中。日常监视不会自动打开这两个页面。

## 2. 总线用途和安全边界

| 总线选项 | 位率 | 用途 | 上位机发送范围 |
|---|---:|---|---|
| CAN1 | 500 kbit/s | 从控原始采样、主控周期状态、F405 工具命令与应答 | F405 工具命令；台架页发送从控帧 |
| CANB | 500 kbit/s | IVT、ECU、Chroma 和 BMS 状态/故障/告警 | IVT 厂商配置命令 |
| CANB Legacy | 250 kbit/s | Legacy 充电机和 BMS 状态/故障/告警 | IVT 厂商配置命令 |

CANB 没有 138 串电压和 48 路温度的原始从控报文。完整单体监视必须连接 CAN1。

- 上位机按本仓库当前 F405 协议运行，不以尚未同步的公共 DBC 作为运行时来源。
- F405 工具命令只从 CAN1 发送。CANB 上位机写通道只用于目标 IVT 的厂商配置和位率切换；BMS、ECU、Chroma 和 Legacy 报文仍按各自接收路径处理。
- IVT 配置使用独立的 Command/Response ID。配置前先确认 PCAN 位率与 IVT 当前位率一致，并确认 CANB 上只有目标 IVT。上位机读取设备信息、模式、8 个通道、10 个 CAN ID 和正负过流阈值，再显示目标核对结果。
- 台架模拟从控工具只在自己的 CAN1 连接上发送六个从控帧；它不发送 IVT 结果帧。模拟数据、历史回放和 BMS 主监视连接都不能执行 IVT 配置。
- 阈值、告警开关、充电请求、电流方向、充电机类型、RTC、Flash 日志清除和故障复位都要求逐次确认。
- 所有工具命令使用统一扩展 ID `0x18A050F5` 和协议版本 4；Byte0 高 4 位是版本、低 4 位是操作码，Byte1 是请求计数，固定 DLC=8。除 RTC 外均核对请求计数、命令码和主控 `0x18A650F4` 统一应答。
- 配置类命令只在自检、待机或故障保持态处理。预充和高压闭合期间由主控拒绝；充电机类型还要求退出充电模式且高压状态机空闲。
- 故障复位仍由主控检查实时告警和实体 HV_ACC 上高压请求是否已释放。上位机只发送请求，不替代主控的安全判断。

## 3. BMS 上位机使用

1. 点击底部“连接上位机”。
2. 选择监视总线和 PCAN 通道。
   - 源码运行时会额外显示 `内置模拟数据 · CAN1 / 开发测试`，用于没有实体 PCAN 时的界面和协议开发；Windows 发布版不提供该项。
   - `CAN1 · F405 主控 / 从控 / 工具`：查看 138 串、48 路温度、F405 状态和发送 F405 工具命令。
   - `CANB · IVT / ECU / Chroma · 500 kbit/s`：查看 CANB 状态、IVT 结果和 500 kbit/s 充电链路。
   - `CANB · Legacy / IVT · 250 kbit/s`：查看 Legacy 固定 250 kbit/s 链路。
3. 连接后先看“运行总览”的状态机、告警和关键电气量；需要定位时进入“电芯与温度”“故障与记录”或“CAN 监视器”。
4. 需要改 F405 参数时进入“参数与命令”。界面会检查 CAN1、主控状态和状态帧新鲜度，每次写入都需要再次确认。PCAN 发送成功不代表主控已经接受，最终以主控应答和周期回报为准。

主控工具命令只从 CAN1 发送。CANB 连接用于读取 CANB 数据，不用于发送 F405 主控配置命令。数据无效、报文缺失或状态帧超时时，页面显示“等待数据”，不使用 0 或旧值冒充当前结果。

## 4. 台架模拟从控

### 连接和启动

1. 展开左侧“工程工具”，进入“台架模拟从控”。
2. 选择连接 F405 CAN1 的 PCAN 通道，点击“连接”。页面固定使用 500 kbit/s。
3. 看到六个从控状态后，再开始注入场景。主上位机需要同时监视 F405 时，可以在另一个主监视连接中打开同一 CAN1；如果 PCAN 驱动拒绝第二个句柄，按提示释放占用或使用另一个 PCAN 通道。

### 页面会发送什么

台架启动后按脚本周期发送：

- 六个从控的 36 帧电压数据，合计 138 串；
- 六个从控的 6 帧温度数据，合计 48 路；
- 不发送 IVT `0x512..0x519`、充电机反馈或 IMD 报文。

六个从控默认在线，电芯和温度使用脚本中的默认值。页面下方的六个状态格用于确认从控在线情况和默认测量值。

### 注入场景

快速按钮用于常用测试。命令框接受脚本的原命令格式，例如：

```text
cell 1 3900
temp 1 40
slave 2 online off
scenario openwire on 1
```

开始下一组测试前点击“恢复默认”。需要验证 F405 真实 IVT、充电机、IMD 或 CANB 报文时，使用真实设备或对应独立工具，不在本页面伪造。

## 5. IVT 配置

### 连接前准备

1. 将 PCAN 和目标 IVT-S 接到 IVT 所在 CANB。
2. 总线上只保留目标 IVT 和 PCAN，暂时移除其他会响应 IVT 命令的节点。
3. 在页面选择 PCAN 通道和 IVT 当前位率。第一次读取出厂设备通常使用 500 kbit/s；如果设备当前用于旧 Legacy 充电机，按设备当前实际位率选择 250 kbit/s。
4. 点击“连接”。PCAN 位率必须与 IVT 当前位率一致，否则不会有有效应答。

### 先读取，再决定是否配置

点击“读取并核对”。页面会先尝试出厂地址 `Command 0x411 / Response 0x511`，再尝试 BMS 目标地址 `0x410 / 0x51A`，随后读取设备信息、当前/上电模式、8 个通道、结果帧地址和正负过流阈值。

核对结果的含义：

| 页面结果 | 含义 | 下一步 |
|---|---|---|
| 已配置且一致 | 当前配置与 F405 CANB 目标全部一致 | 可以断开配置通道，接入 F405 验证结果帧 |
| 未配置 | 读取到出厂通道、地址和关闭过流阈值 | 首次接入 F405 前执行一次 BMS CANB 配置 |
| 配置不符 | 只有部分项目一致，或存在被改过的参数 | 展开逐通道核对，确认后再配置 |

页面主区域只显示现场需要的结果。逐通道周期、格式和差异放在“查看逐通道核对”中；当前 Command/Response ID 和过流核对值放在“高级”中，通常不需要修改。

### 配置为 BMS CANB

当设备是首次安装或核对结果为“配置不符”时：

1. 确认总线仍只有目标 IVT，且 PCAN 位率与 IVT 当前位率一致。
2. 如有需要，在“高级”中填写目标上电模式和过流阈值；过流值为 0 表示关闭。
3. 点击“配置为 BMS CANB”，在确认窗口核对通道、位率和目标地址后确认。
4. 页面会停止 IVT，写入 BMS 目标配置，保存并重启 IVT，然后自动等待 Alive 和重新读取。
5. 看到“已配置且一致”后，断开 PCAN，按 [`IVT能量计配置与CANB切换.md`](IVT能量计配置与CANB切换.md) 的步骤接入 F405 CANB。

页面会先要求完成“读取并核对”：读回前配置按钮保持禁用；如果结果已经是“已配置且一致”，配置按钮也会保持禁用，避免重复写入。

BMS 目标的现场摘要为：结果帧 `0x512..0x519`，通道格式 `0x42`（小端、周期发送、正常符号、报错位关闭），Command `0x410`，Response `0x51A`。周期和每个通道的完整字段见 IVT 文档。

### 旧 Legacy 充电机的位率切换

“旧充电机过渡”只改变 IVT 的 CAN 位率，不改变结果通道和 CAN ID：

- 旧 Legacy 充电机链路需要 250 kbit/s 时，点击“切到 250 kbit/s”；
- 回到 F405 当前 500 kbit/s CANB 链路前，点击“切到 500 kbit/s”。

点击后 IVT 会重启，上位机会关闭当前 PCAN、按目标位率重新打开并等待 Alive。切换前后的连接位率必须与设备实际位率匹配。Legacy 固定变体的安装位率由整车配置决定，运行中不反复切换。

## 6. 常见提示和处理

| 提示或现象 | 处理 |
|---|---|
| 等待 CAN 数据 | 检查主监视连接、PCAN 通道和位率；数据无效时页面保持“等待数据”，不会用 0 或旧值代替 |
| 台架连接失败 | 检查 PEAK 驱动、PCAN 通道是否被占用，以及通道是否确实接到 F405 CAN1 |
| IVT 读取超时 | 确认 IVT 供电、当前位率和总线接线；出厂设备用 0x411/0x511，已配置设备让页面自动探测或在高级中填写当前地址 |
| IVT 配置不符 | 查看逐通道核对；确认设备确实是要接入本 F405 的 IVT 后再重新配置 |
| 切换位率后没有 Alive | 用目标位率重新连接；仍无响应时回到原位率检查设备和接线 |
| 第二个独立页面连接失败 | 可能是 PCAN 驱动不允许同一通道同时打开多个句柄；先断开占用连接或换用另一个 PCAN 通道 |
| 主控命令被拒绝 | 查看“参数与命令”的主控状态、告警和应答原因；预充或高压闭合期间不要反复发送 |

故障、数据失效和状态帧超时使用不同的醒目状态。正常状态保持安静；看到“等待数据”时先处理连接或报文问题，不要按测量值为 0 继续判断车辆状态。

## 7. 验证顺序

### 软件和界面

1. 源码运行上位机，确认主监视连接、工程工具折叠项和两个工具页都能打开。
2. 启动台架模拟从控，确认 F405 收到 138 串和 48 路数据，注入过压、欠压、温度、离线和断线场景，确认主控告警和页面状态一致。
3. 用真实 IVT 完成“读取—分类—配置—重读”流程，确认显示“已配置且一致”。

### 实体联调

1. 先在低压、无高压输出条件下验证 PCAN 通道和位率。
2. 配置 IVT 后断开配置 PCAN，再接入 F405 CANB，检查 `0x512..0x519` 和 IVT 离线告警。
3. 旧 Legacy 链路分别验证 250 kbit/s 和回切 500 kbit/s 的 Alive、结果帧周期和 F405 接收状态。
4. F405 工具命令、故障复位、Flash 日志和高压动作按 [`调试与台架测试.md`](调试与台架测试.md) 执行；本文不重复固件内部判定规则。

项目级自动测试已覆盖从控映射、主控状态、故障与告警、工具命令编码和应答、IVT 协议与核对、CANB 写入边界、模拟链路和记录/回放。实体 PCAN、真实 IVT、F405 台架、充电机、Windows 发布包和长时间记录仍需现场验证；详细检查项见 [`TOOLS/bms_host/todo.md`](../TOOLS/bms_host/todo.md)。

## 8. 文件职责和维护位置

| 内容 | 维护位置 |
|---|---|
| F405 CAN ID、DLC、字段、周期、超时和协议兼容规则 | [`CAN通信协议.md`](CAN通信协议.md) |
| IVT 设备目标配置、结果通道和 CANB 位率切换 | [`IVT能量计配置与CANB切换.md`](IVT能量计配置与CANB切换.md) |
| 上位机、台架和 IVT 配置的使用流程 | 本文 |
| 上位机安装、运行和 Windows 发布 | [`TOOLS/bms_host/README.md`](../TOOLS/bms_host/README.md) |
| 上位机待办、风险、验证和已完成变更 | [`TOOLS/bms_host/todo.md`](../TOOLS/bms_host/todo.md) |
| F405 固件进度、跨层联调摘要和固件风险 | [`../todo.md`](../todo.md) |

独立命令行工具仍保留：[`pcan_bms_bench.py`](../TOOLS/pcan_bms_bench.py) 和 [`pcan_ivt_tool.py`](../TOOLS/pcan_ivt_tool.py)。它们与上位机遵守相同的 CAN1/CANB 使用边界。

上位机界面细节和文件格式不在其他 `DOC/` 文档重复维护；只有影响 F405 协议、安全限制或整车联调结论的变化，才同步修改本文、CAN 协议文档和对应 todo。
