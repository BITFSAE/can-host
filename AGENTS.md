BITFSAE 车队 CAN 上位机（can-host）：BMS 监视 + 整车 CANB 遥测的桌面工具，Python（PyWebView + python-can）+ 零构建前端，面向 Windows + PCAN-USB 发布。

## 接口权威仓库

兄弟仓库都与本仓库同级存放（本地路径 `../<仓库名>`，GitHub 组织 BITFSAE）：

- BMS 帧（CAN1/CANB 全部 F405 报文）以 BMS-MASTER-F405 固件 `App/bms_can_protocol.c` 和其 `DOC/CAN通信协议.md` 为准。
- 整车风扇 `0x5A2..0x5A7` 以 FanController 固件 `Core/Src/fan_controller.c` 和其 `Doc/风扇控制.md` 为准。
- PDM 低压 `0x5A0/0x5A1` 以 PDM 固件 `Core/Src/pdm_monitor.c` 和其 `Doc/CAN接口.md` 为准。
- 整车各节点 DBC 的团队中央登记在 vehicle-interfaces 仓库 `can/Vehicle_CanB.dbc`；固件与 DBC 不一致时以固件为准，同步修改本仓库实现、测试和 vehicle-interfaces。

任何帧格式变更都要同步：`canhost/decoders.py`（唯一定义）→ 使用它的协议状态机 → `Tests/` 对应测试 → 必要时 `DOC/CAN上位机与工具使用.md`。`Tests/test_fan_controller.py` 与 FanController 文档示例帧逐字节锚定，不得放松。

## 工作原则

- 稳定性优先：功能正确、完整，模块协作可靠。安全相关路径（BMS 命令门控、风扇命令确认、总线写入边界）改动时条件逐字保留并保持测试锚定。
- 上位机是纯消费端：不定义新 CAN 契约；发现实现与固件不一致时先核对固件，再修上位机。
- 每种帧格式只允许一个定义（`canhost/decoders.py`）；新增节点解码先在 decoders 加纯函数和测试，再接入协议状态机与页面。
- 页面模块化：前端 `canhost/web/js/` 按页面拆分（core/bms/vehicle/fan/bench/ivt），保持零构建、多 script 直载；新页面按同一模式注册，不回退到单文件。
- 交付物是完整、生产可用的车队工具；临时调通代码不是目标。

## 文档要求

- 代码实现优先于文档；代码行为变化时更新 `DOC/` 下对应文档并在 `DOC/README.md` 登记。
- 按“职责 → 已实现流程 → 接口/协议 → 异常 → 调试验证”顺序组织；已实现行为与后续工作分开，待确认参数和已知不足集中在文档末尾。
- 标题直接说明本节内容；用常用短句，完成条件尽量用可观测信号表达；澄清只加在读者可能真实误解的位置并给出原因。
- 根目录 `todo.md` 记录上位机的进度、待办、风险、验证和已完成变更（原 `TOOLS/bms_host/todo.md` 迁移而来）。固件仓库的 `todo.md` 归固件仓库，两边互不重复，跨层联调摘要各自留条目并指明对方位置。
- 只修改 `AGENTS.md`、`.codex/` 等 AI 工作流配置时不必更新进度文件，除非同时改变项目实现计划或验证结论。

## 上位机界面

- **设计风格与视觉层次**：
  - Pit-wall 赛车维修区 / 工业遥测暗色工程风格，信息密度与确定性状态表达优先于纯装饰；
  - 数据与测量值（等宽字体、高字重）显著突出于次级标签与单位；同一层级字段的字号、字重、对齐与间距保持严格一致；
  - 正常状态保持安静（暗绿/低饱和度），故障、告警、数据失效与超时高对比度醒目区分（红/黄/橙）；数据无效或过期时明确显示“等待数据”，严禁使用 `0` 或旧值冒充当前读数；
  - 状态机信息条与其他卡片使用相同的边框、圆角和间距，不添加彩色左侧竖条或只在单个区域出现的装饰。
- **页面与连接模型**：
  - 四类连接互相独立：BMS 主监视（CAN1/CANB/模拟/回放）、整车（CANB/模拟）、台架（CAN1 注入）、IVT 配置（CANB）；进入一个页面不会切换另一条连接的模式；
  - 一个卡片只承担一个主题（如 SOP 限功率、PDM 低压、ECU 四轮各自分开）；整车总览按节点分卡，后续新增节点沿用同一卡片框架；
  - 底部常看值快捷栏在所有非工具页常驻，数据缺失显示“等待”；高压/SOC 优先主连接、回退整车连接 `0x4B0`；
  - 工程工具页面（台架、IVT 配置）采用顶栏专属连接控制并隐藏底栏；整车页面用顶栏收纳整车连接控制，底栏保持可见。
- **文字与信息去冗**：
  - 严禁在操作界面大段搬运协议或固件说明文档；输入框依靠字段名、单位后缀（`°C`、`%`、`V`、`A`、`s`）及 HTML5 校验属性自解释；
  - 动作按钮文字使用简明动作动词（如 `写入保护阈值`、`下发模式命令`、`配置为 BMS CANB`）；
  - 鼠标悬浮提示只补充短的字段解释、协议缩写或机制说明（如 PDM 能量回绕），不显示清零值、无效值、超时兜底等实现细节。

## 子代理分工

仅在用户明确提及使用子代理/subagents 时才主动使用子代理；用户未提及时，所有分析、修改和复核都由主代理直接完成。

DO NOT send optional commentary
