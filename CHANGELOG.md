# 变更记录

本文件记录已完成的版本结果和兼容性变化。未完成验证、风险和后续工作见 [`todo.md`](todo.md)。

## 未发布

## v0.9.13 — 2026-09-19

- 风扇自动标定不再把正常背景负载波动误判成安全故障：PDM 电流/功率标准差超出质量门槛时保留测点、标记待复核并继续扫频，只从自动推荐上限中排除；有效样本不足以及电流、温度、供电、遥测和固件安全中止等硬条件仍会停止。标定一开始即可导出，首个基线或首个测点前中止时 CSV 仍带会话状态与原因，JSON 同时保留已采原始样本和质量告警。
- 底部 CAN1、BMS CANB、整车 CANB 三个连接按钮不再在连接成功后强制跳转到 CAN 监视器；当前页面保持不变，监视器仍自动选中刚建立的数据流。

## v0.9.12 — 2026-09-19

- macOS 软件更新改为与 Windows 一致的两步流程：点击下载后获取、校验并解压签名应用包专用的更新 ZIP，按钮随后切换为“重启更新”；独立 shell 助手退出旧版、交换整个 `.app`、启动新版并核对进程与版本健康信号，失败时自动回滚。应用所在位置不可写时才回退为浏览器下载 DMG 后手动覆盖。
- 修复 F405 电池箱风扇因安全看门狗先行中止时的收尾误报：新鲜的“已中止、目标归零”加 STOP 应答可直接确认安全停止；普通中止流程也接受固件回报的未激活/已中止零目标终态，不再把安全退出写成“停止命令失败”。配套 F405 固件把 STOP 改为幂等，重复命令保留完成提交资格或原始中止原因。

## v0.9.11 — 2026-09-18

- 修复 FanController 因安全看门狗先行中止时的恢复误报：收到新鲜的“已中止、目标归零”状态后，STOP 应答即可确认安全收尾，不再等待固件协议上不会出现的“已完成”帧并追加“固件恢复失败”。配套固件同时修复 PDM 接收中断与控制循环之间的时间戳竞态，避免正常 100 ms 遥测被误报超时。
- 修复 Windows 系统代理残留导致更新检查持续报 `WinError 10061`、只能重启电脑恢复：更新检查与下载不再使用进程级缓存代理，每次读取当前设置；本地代理端口拒绝连接时自动直连公开更新源重试一次，仍失败则给出明确的系统代理处理提示。

## v0.9.10 — 2026-09-18

- 修复 FanController 自动扫频在第一个 5% 测点提前结束：低占空比 0 RPM 现在作为“尚未起转”的测量结果记录，不再被上位机直接当成堵转中止；配套固件会持续保持低占空比扫描目标，避免启动确认失败后在目标与 0% 间反复重试而污染功率采样。真实运行停转仍由固件安全看门狗通过 `0x5A9` 中止，温度、供电和总线电流保护不变。

## v0.9.9 — 2026-09-18

新增深浅主题，并修复软件更新检查的偶发误判：

- 修复检查更新偶发误判：CNB 与 GitHub 改为并行核对并选择最高有效语义版本，同版本仍优先使用 CNB 下载；Release 列表不再依赖远端顺序，无效标签不会遮挡有效版本；前端更新状态轮询改为单飞并捕获 WebView 桥异常。下载、安装和重启流程不变。
- 新增浅色主题及侧栏太阳/月亮切换：浅色采用冷灰画布、白色测量面和适配亮背景的状态色，不对暗色界面做简单反相；页面、表单、弹窗、底栏、滚动条、品牌字标和两张 Canvas 趋势图共用主题语义变量。选择即时保存到 WebView 与应用设置，下次启动会提前应用并同步原生窗口底色；切换动画遵从系统“减少动态效果”设置。
- 新增主题前端连线、持久化和原生启动底色测试；浏览器预览覆盖 1460×920 深浅总览、浅色电芯、过期风扇、连接弹窗及 1120×720 窄侧栏，修正主题适配时发现的原生下拉箭头重复平铺问题。

## v0.9.8 — 2026-09-15

PCAN 通道改为按实际接入的设备枚举：

- PCAN 通道选择不再固定列出 `PCAN_USBBUS1..8`：后端通过 PCAN-Basic `PCAN_ATTACHED_CHANNELS` 枚举当前实体通道，界面显示设备名、设备内 CAN 序号及可用/占用状态，并在连接设置提供热插拔后的“刷新设备”。Windows 枚举成功但无设备时显示明确空状态且禁止用默认句柄误连；不支持该参数的旧版 MacCAN/PCAN-Basic 保留手动通道列表并明确标出兼容回退。
- BMS 主连接、整车连接、台架、IVT 与本地遥测模拟器共用同一份动态 PCAN 列表；实体连接没有选择通道时，后端也会拒绝隐式回退到 `PCAN_USBBUS1`。

## v0.9.7 — 2026-09-14

本地遥测模拟器进入 Windows 发布包，并清掉打包规格里失效的排除项：

- “工程工具 → 遥测数据模拟”不再只随源码运行和 macOS 过渡版发布：Windows 安装版同样带这一页（MQTT / 串口 / 独立 PCAN 三种输出齐全）。它此前被 `_simulation_available()` 连带挡住，而这个判定服务的是另一件事——实体 CAN 的调试模拟通道。现在两者分开：遥测模拟器无条件随包，调试模拟通道仍只在源码运行和 macOS 过渡版提供，Windows 发布版保持硬件专用（界面不显示“调试模拟”开关，后端对 `mode=simulation` 仍返回“当前发布版仅支持真实 PCAN 设备”）。
- Windows 打包规格随之补齐：`hiddenimports` 增加 `canhost.telemetry.simulator`、`canhost.bms.simulator`（遥测模拟器的 CAN 帧定义来源）、`serial` 与 `serial.tools.list_ports_windows`。此前 `excludes` 里的 `canhost.simulator` 是个在 `canhost/bms/simulator.py` 重命名后就失效的名字，规则一直空转，所以 BMS 与整车模拟器其实都进了 Windows 包且从未被核对；现在排除项改为真实存在的 `canhost.vehicle.simulator`，本机对改后的规格跑 PyInstaller 分析确认：整车模拟器不在包内，BMS 模拟器、遥测模拟器和 pyserial 都在。
- Windows 构建补上冻结包自检（此前只有 macOS 构建运行）：`build_windows.ps1` 打包后以 `--packaging-smoke-test` 运行产物并核对退出码。自检本身也按平台分开——两个平台都要有可用的遥测模拟器、pyserial 和完整 `TelemetryFrame`，macOS 还要能启动调试模拟通道，Windows 则必须既不开放该通道标志、又要真的拒绝 `mode=simulation` 连接。
- 新增 `Tests/test_packaging_spec.py`：断言两个 spec 的 `excludes` 只能写真实存在的模块（防止同类失效名字再出现），且两个发布包的 `hiddenimports` 必须显式包含遥测模拟器与 pyserial。

修正表头与读数错列、分组标题缩进和分割线两侧间距：

- 整车总览“ECU 四轮状态”表的表头与读数错开一列宽：表头标签左对齐（`扭矩 %Mn` 起于列首），四行读数右对齐（按列宽贴到列尾），实测两者在同一列内相差约 170px。读数改为与表头同边左对齐。曾用于右对齐表头的 `.veh-ecu-head .num` 从未被任何标记使用，一并删除。右对齐表头同样能对齐，但窗口 1460px（整车两列布局）时列宽只剩约 85px，右对齐会让读数与右邻表头只隔 8px，所以统一取左对齐。
- 整车总览“六个模组”表最后一列同样错列：读数（`新鲜/已过期/缺失`）贴右端，表头“状态”却靠左，同一列内相差半个列宽；表头改为与该列读数同边。
- 面板内分组标题缩进过深：`.sub-head` 自带 12px 左右内边距（为 `.panel-body.flush` 这类无内边距容器准备），放在已有内边距的 `.panel-body` 里就叠成两层，风扇页“1 阶梯扫频”“2 保存两档上限”“1 自动阶梯扫频”“2 保存上限”“3 手动单步”“命令应答记录”的标题文字因此比下面的字段标签右移 38px。现在这两类容器里的标题左右不缩进，与字段同一条左边界。
- 分组标题的下边框与首行内容之间此前为 0（字段标签紧贴分割线），现留出 `--sp-2`；标题下边框与分组首行的左右范围一致（实测标题、字段网格、进度条、记录表都是同一条 256.2–1416.2 内容边界）。

风扇标定与手动调试指南重写，界面用词与 PDM 权威文档统一：

- 指南按两套系统的实际操作重写：两套标定共同的现场条件、采样规则、命令确认和中止收尾合并为第 3 节，FanController 与 F405 各留专章；新增固件如何使用上限（未标定按 15%/55% 封顶、按供电档位选择上限、Flash 双页写入与擦除期间 20~40 ms CPU 停顿）和“已知不足”小节（功率预测表仍是占位值、Chroma 上限需要人工复测、双回路联合扫频未开放）。
- 修正电池箱风扇提交命令的字段位置：`0x5AB` 操作码 `0x04` 的 Chroma 与高压上限在 Byte2/Byte3，Byte4/Byte5 是固定值 35 与 70，Byte6 才是确认字 `0xA5`；此前只列了 Byte4..Byte6，读起来像是两个上限在更后面。核对固件 `App/bms_fan.c` 的 `BMS_FAN_CAN_CMD_COMMIT_LIMITS` 分支确认。
- 界面用词与 PDM 文档统一：PDM 卡片的“OR-RING 后整车负载”改为“OR-ing 后的整车负载”（PDM `Doc/CAN接口.md` 用的就是 OR-ing），`0x5A8` 供电状态的悬浮提示改为“供电来源与限功率判定”，电池箱风扇“保存上限”的提示改为“由 PDM 功率读数扣除 0% 基线后给出；停止扫频会话后再提交”。

## v0.9.6 — 2026-09-14

界面几何与读数组件统一（整车两页重构）：

- 全站几何收敛到一套令牌：圆角只保留三档（结构 0、可交互与标记 2px、状态点圆形），字号 6 档（`--fs-unit/label/body/value/value-lg/kpi`），间距 5 档（4/8/12/16/24）；此前 9 种圆角取值、357 处写死字号和 579 处写死间距已收敛（只剩 `inherit`、1px 缝隙和微型徽标这类光学例外），并删除 12 处装饰渐变与全部结构性阴影，卡片头高度统一为 40px。
- 只读读数统一为 `.readout-grid` 单元网格：单元格之间只有 1px 缝隙，没有独立底色、边框和圆角。BMS 运行总览、整车总览与风扇遥测共用这一种写法；BMS 侧的继电器、状态表、条件栏和模组表改用同一实现，删掉逐格描边与 `nth-child` 例外（这些例外此前在多处叠成 2px 双线）。
- 整车总览改为两个显式列栈、趋势图整行收口在最下：跨列卡片会把栅格切成行、让短列无法回填，这是此前右栏 404px 空洞的成因。改版后左右两列高度差小于 90px，页面高度从 1105px 降到 802px。读数单元（限值、标记、ECU 确认、赛会通道、PDM 两侧、胎温测点）不再各自成盒，子块标题只保留 1px 分隔线。
- 整车风扇页改为三带结构：顶部动力总成冷却遥测横条、整行两档功率标定（落在首屏，按“阶梯扫频 / 保存两档上限”两个步骤分区，步骤标记用方框序号而不是 ① 这类字符）、下方按对象分两列（左列整车 FanController、右列 F405 电池箱风扇）。
- 整车风扇页的间距与字段宽度按同一套原语重排：参数一律进 `.field-grid`（列数显式声明），动作一律另起一行右对齐（`.panel-foot`），不再与字段挤在同一行。同一行字段因此等宽（双列 273px、三列 178px、四列 280px），而此前同一行内宽度在 43–178px 之间跳动，实测把“35”裁成“3”、“20%/s”裁成“%/s”、“固定保底”裁成“固定保”；温控曲线卡按主题拆成“自动温控曲线”和“温度失联策略”两张卡，各组当前值报告移到卡片标题右侧；步骤说明进悬浮提示，界面上只留当前状态。代价是主区滚动高度从 1463px 回到 1945px（1680×1000）：字段标签与动作各占一行，换来的是每个输入框都能读全。
- 读数单元内部结构统一：数据年龄、目标占空比、转速结论一律作为贴右端的 `.cell-tail`，温度来源有效位两路合并到同一个容器，不再各自漂在不同横坐标上；遥测横条按“温度与供电 / 风扇转速与回路输出 / 自检与故障诊断”分组，列数改为整除单元数（故障位 8 项 4 列 × 2 行），窄窗口不再露出网格底色形成的空单元。
- 修正风扇页过期态失效：此前把 `data-stale` 加在读数单元内部的 `<b>` 上，而配色规则只认单元格，风扇页的过期配色实际没有生效；现在统一标记整个读数单元，过期读数退到次级灰、单位和尾部状态转告警色，转速状态显示“已过期”、目标占空比显示“上次目标”。
- 整车风扇页的写入锁定判定与页面按钮一致：内置模拟通道原先被当成可写通道，既不显示提示条也不锁定按钮，现在按 `mode === "pcan"` 判定，模拟连接下显示“当前整车连接是内置模拟数据；风扇命令需要实体 CANB”并锁住写入按钮，只有实体 CANB 500 kbit/s 才算可写。
- 用图形替代字符图标：侧栏“工程工具”折叠箭头、更新弹窗的版本对比箭头、弹窗关闭叉和下拉框指示改为 1px 折线（下拉指示原先是两个 4px 渐变三角）；字段输入框与下拉框在同一行统一到同一高度与字号。
- 帧来源与固件细节移入悬浮提示：卡片标题右侧只报告数据本身（“等待数据”“部分已过期 · 最旧 N s 前”），帧 ID、字节序、周期和“发送查询后 F405 在限时窗口内回报状态”一类机制说明不再常驻界面；标定未就绪等提示改为“等待 PDM 低压功率数据”“等待风扇遥测与标定上限帧”“PDM 实测未证明 DCDC 已接管”这类不带帧号的表述。
- 清理 `styles.css` 中指向已删除标记的失效规则并修正风扇页一处多余的闭合标签——该标签曾把“CAN 监视器”和“遥测故障”两页移出 `main`，导致这两页布局塌陷；失效规则上轮清掉 119 条，本轮按“选择器里只要有一个类名在前端任何文件里都不存在”再清掉 29 条，清完用逐元素计算样式快照比对 11 个页面，渲染结果与清理前完全一致。
- 新增 `scripts/ui_preview.py`：用包内确定性模拟器生成快照、在浏览器里打开真实前端（`live / stale / calib / sim` 四种状态），不需要 PCAN 硬件即可复核间距、换行、过期配色和控件宽度。

### 更新与发布

- 升级后的首次启动显示“更新完成”弹窗：并列更新前后的版本号和发布日期，逐条列出本次更新内容，并可打开对应版本的发布页或浏览版本历史；安装包覆盖安装和软件内更新走同一条路径，不会再出现升级后没有说明的情况。
- 软件更新弹窗的版本说明改为按条目显示（不再把 Release 正文整段塞进窄框），并新增可折叠的历史版本列表；说明缺失时提示正文原文或发布页入口。
- 版本说明只保留一个来源：`CHANGELOG.md` 的对应小节由 `scripts/set_version.py` 写进程序、由 `scripts/release_notes.py` 生成 Release 正文，构建两个平台时自动生成，不再手工分别维护。

## v0.9.5 — 2026-09-13

- 将 CANRS485_G473 与 CAN2RS485 中内容一致的 `local_sim2.py` 整合为“工程工具 → 遥测数据模拟”：支持 MQTT、串口和独立 PCAN 同时输出，显示运行状态、实时模拟值与发送计数；PCAN 路径复用现有 BMS 模拟帧定义，macOS 包新增 pyserial 和模拟器打包自检。
- 修复 macOS 发布包检查更新时报 `CERTIFICATE_VERIFY_FAILED`：更新器 HTTPS 与 MQTT TLS 在系统信任库上追加随包 certifi CA 证书，打包自检增加证书包检查，检查与下载阶段均提供可执行的报错建议；本地遥测模拟器等待 Broker 确认连接后才进入发送状态。
- 电芯与温度、整车总览、PDM、两套风扇及底部快捷栏不再把超时的正常样本瞬间清成横线；保留本次连接的最后有效值并显示数据年龄和“已过期/旧”状态，同时保持固件新鲜度、控制安全门控、活动故障、断线码和无效值判定不变。

## v0.9.4 — 2026-09-12

- CAN 连接、MQTT 订阅和软件更新弹窗支持点击遮罩空白处关闭；高风险命令确认弹窗仍要求通过取消按钮、关闭按钮或键盘退出，避免误触遮罩丢失核对上下文。

## v0.9.3 — 2026-09-12

- 修复 Windows 安装版从快捷方式启动后，更新助手继承安装目录作为工作目录，导致安装目录始终处于占用状态、无法替换的问题。
- 安装目录重命名增加最长约 15 秒的有限重试，覆盖主进程退出后 WebView2、杀毒软件或文件系统仍短暂持有句柄的情况。
- 升级提示：v0.9.1/v0.9.2 自带的旧安装助手不能被下载包反向修复，需要手动运行一次 v0.9.3 Setup；此后软件内更新会使用新版助手。

## v0.9.2 — 2026-09-12

- 软件更新入口改为下载与重启两步：侧栏显示下载图标，下载中显示进度、大小和速度，校验完成后由用户点击“重启更新”。
- 完善两套风扇标定安全边界：仅允许真实 CANB 500 kbit/s、双回路完整性、PDM 测量互斥、ACK 后新状态帧及采样期目标核对、旧线程退出和断连收尾、非单调结果保守截断、无有效点不生成假建议，以及标定表单输入和状态保护。
- 修复标定正常流程摩擦：电池箱风扇提交按钮按最后已知完成状态保持可用；样本不足或波动过大时延长一轮复采；扫频中止提示补充记录导出与手动保存路径。
- 新增 Apple Silicon macOS arm64 应用包、DMG、SHA256、自检和 ad-hoc 签名流程；GitHub Release 同时构建 Windows 与 macOS，并将 5 个附件同步到 CNB。
- CNB 限流兜底可探测 macOS 附件；兼容历史 Windows-only 发布，并拒绝不完整的 DMG/校验文件组合。
- 重整项目文档职责，README 只保留项目入口，开发发布、使用、待办和变更记录分别维护。

## v0.9.1 — 2026-09-12

- 软件更新合并为一次确认；下载、校验、退出、替换和重启自动完成。
- 新版须在 45 秒内回报匹配的进程号和版本号；失败时终止新版、恢复旧目录并重启旧版。
- 更新日志迁至固定目录并保留最近 20 份；弹窗内错误提示不再被遮罩覆盖。
- 更新检查优先使用 CNB 国内镜像，失败时回退 GitHub；GitHub 令牌不会发往 CNB。
- 新增 GitHub/CNB 代码与发布镜像工作流，以及 CNB 定时补镜像任务。

## v0.9.0 — 2026-09-04

- CAN 监视器改为通用双连接数据源，支持 ID 汇总、内容变体、筛选、留档、CSV 和回放。
- 新增持久化单发/周期发送；项目已知命令 ID 继续由专用页面处理。
- 完成 Windows 安装器、便携 ZIP、SHA256 和旧版本清理流程。

## v0.8.3 — 2026-09-04

- 修复 Windows PowerShell 5.1 中文脚本编码和 Inno Setup 卸载清理兼容问题。

## v0.8.2 — 2026-09-04

- 同步 F405 告警开关帧版本 7 和 23 位动作开关模型。

## v0.8.1 — 2026-09-03

- 单体电压 `≤2100 mV`、`≥5400 mV` 或 `0xFFFF` 按单路断线处理，不影响同帧其他通道。
- 增加两套风扇的标定与手动调试指南。

## v0.8.0 — 2026-09-03

- 底栏形成 BMS CAN1、BMS CANB、整车 CANB 三个连接入口；BMS 主服务与整车服务保持独立。
- 修正快捷栏数据回退、整车计数和风扇状态显示。

## v0.7.0 — 2026-09-02

- 接入 FanController 与 F405 电池箱风扇的功率标定、安全中止和两档上限保存。
- MQTT 遥测故障页改用校验后的 `fsae/telemetry/v1` 数据。

## v0.6.0 — 2026-09-01

- 同步 F405 工具协议版本 5、动作型告警开关、350 ms 从控窗口和默认电流方向。
- 从控电压、温度改为逐通道有效性处理。

## v0.5.1 — 2026-08-31

- 修复 Windows 软件更新后新窗口不出现；增加旧目录备份和启动失败回退。

## v0.5.0 — 2026-08-31

- 自有 IVT-S 从 CANB 迁移到 F405 CAN1；IVT 工具和页面同步调整。

## v0.4.1 — 2026-08-30

- IVT 八个结果通道周期支持编辑、写入、重启和逐项读回。

## v0.4.0 — 2026-08-30

- 同步 BMS CAN1/CANB 协议、Chroma 反馈和 SOP 发送条件。

## v0.3.0 — 2026-08-29

- 新增 MQTT 故障订阅、风扇功率仲裁遥测、自动标定和安全边界。

## v0.2.0 — 2026-08-25

- 修复 PyWebView 模拟数据和整车趋势显示；统一产品名称为 BITFSAE CAN Host。

## v0.1.0 — 2026-08-20

- 从 BMS-MASTER-F405 工具目录迁出为独立仓库。
- 建立 BMS 监视、CAN 记录回放、F405 工具命令和 Windows 打包基础。
