# BITFSAE CAN HOST 进度、待办与变更

本文档记录车队 CAN 上位机的剩余工作、风险、验证结果和已完成变更。原 `CHANGELOG.md` 的内容已经并入文末“已完成变更”节，本仓库只保留这一份进度文件，不再单独维护 changelog。上位机进度、风险和验证以本文件为准；固件仓库（BMS-MASTER-F405）的 `todo.md` 只记录固件侧内容和跨层联调摘要，两边互不重复。使用者说明见 [`DOC/CAN上位机与工具使用.md`](DOC/CAN上位机与工具使用.md)，安装和发布见 [`README.md`](README.md)。

本仓库由 BMS-MASTER-F405 的 `TOOLS/bms_host` 于 2026-08-24 迁出并扩展为车队 CAN 上位机；迁移前的历史记录保留在文末“已完成变更”节。

## 当前状态

- 已同步 F405 的告警开关 v5 协议：上位机按整组 23 个开关写入，基础安全保护关闭后主控仍检测并上报告警，只是不再触发动作；正式固件会拒绝关闭强制安全位。故障帧 bit22 保留，外部安回事件移到高压诊断位；上位机把该事件显示为调试提示，不再显示成一级 BMS 故障。读取旧版故障帧版本3时仍按旧 bit22 名称显示。告警开关协议增加两个从控 HV_ON 动作开关，四个特殊开关只影响 HV_ON 后的保护动作，主机默认保留所有普通安全位。Chroma 节点基准已改为 `0x200`，六个实际 ID 为 `0x201/0x202/0x204/0x205/0x290/0x291`，并由 `canhost/decoders.py` 统一派生；后续改面板地址时与固件、遥测网关和共享 DBC 同步。
- CAN1、CANB 500 kbit/s、Legacy CANB 250 kbit/s和内置模拟数据已经接入。
- 已实现138串电压、48路温度、主控总览、故障与记录、独立参数与命令页、CAN记录/回放；高压、温度/冷却和 IMD 已按现场判断顺序分成直接可读的卡片。使用字段和页面顺序见 [`../../DOC/CAN上位机与工具使用.md`](../../DOC/CAN上位机与工具使用.md)。
- 在线写命令逐次确认，并核对总线、主控状态、请求计数、命令码和统一应答。
- F405 工具命令发送前核对运行诊断中的协议版本；主控版本不是5时直接说明版本不匹配。`0x18A650F4` 即使来自旧协议也保留用于诊断，避免把明确的版本拒绝显示成超时；Flash 结果码7按十六进制显示主控返回的芯片 ID，运行诊断显示 Flash 离线时禁用持久化配置按钮。
- 台架模拟从控和 IVT 配置已经拥有独立页面、独立连接和独立服务；默认收在“工程工具”折叠项中，不再通过底部连接方式切换。台架固定向 F405 CAN1 发送从控帧；真实 IVT-S 也接 F405 CAN1，台架不发送 `0x512`。
- 风扇工具页已接入两套风扇：FanController `0x5A2..0x5A9/0x5AE`支持低压电池与DCDC两档自动阶梯扫频、CSV/JSON导出、两档Flash保存和15%未标定上限；F405电池箱风扇`0x5AA..0x5AD`支持限时查询、租约控制、高压标定步骤、35W/70W上限提交与保存状态复核。两套命令都按操作码和序号等待应答，PDM电流方向按实测正=放电。
- 已同步 F405 协议版本5：所有工具请求使用 `0x18A050F5`、固定 DLC=8；CANB BMS 状态/故障/告警使用 `0x4B0/0x4B1/0x4B2`，Chroma 反馈使用 `0x201/0x202/0x204/0x205`；`0x186050F4` 按有符号电流和有效位解析。无效或不完整的单体/温度极值不再显示为占位值或旧值；总览不再把故障帧当作总状态帧，状态帧过期或有效位为 0 时显示“等待数据”；后台写命令也拒绝没有新鲜总状态帧的发送。
- 参数页已同步 F405 的单体过压范围：默认 4190mV；上位机输入允许到 4500mV，是否接受由固件身份决定。Debug-Bringup 写入 4500mV 后下次上电继续生效，正式固件仍拒绝超过 4190mV。Chroma 故障下关输出保持由 F405 固件执行，上位机以 `0x291`、`0x205` 和故障/高压状态帧观察结果。
- 工具页保留脚本的常用场景和命令输入；IVT 先读回并自动尝试出厂/目标 ID，再显示“已配置且一致”“未配置”或“配置不符”，完整配置和 250/500 kbit/s 过渡操作分开显示。
- IVT 页面直接显示并允许编辑八个结果通道周期，随完整 BMS CAN1 配置写入、重启和逐项读回；默认 I 20 ms、其余七通道 100 ms，实时显示合计帧率。出厂周期定义单独保留，用于准确识别未配置设备。
- 主监视连接仍位于底部状态栏，只负责 BMS 监视；台架和 IVT 工具页有自己的 PCAN 通道和连接按钮。
- 整车连接（独立 CANB 通道）已接入：整车总览页显示 SOP 限功率（0x4A0/0x4A3/0x4A4 含配对 CRC 与 ECU 确认）、BMS 整车镜像（0x4B0/0x4B1）、赛会能量计（0x521/0x522/0x526/0x528 大端，功率/能量帧可能不发送）、PDM 低压两侧（0x5A0/0x5A1，四字段全无效值判离线）、ECU 四轮调试帧（0x502/0x505–0x509）和胎温（0x071–0x074，轮位映射待实物确认）；自有 IVT-S 已迁到 CAN1 主监视，整车风扇页并入整车连接。
- 底部常看值快捷栏在所有非工具页常驻：低压 V/I/P、高压 V/I、SOC、SOP 放电/充电功率限值、风扇最高转速；高压和 SOC 优先 BMS 主连接、无数据时回退整车连接 0x4B0。
- CAN 监视器支持主连接/整车连接数据源切换。
- MQTT 遥测故障页已接入：第五条独立只读连接默认订阅 CRC 已通过的 `fsae/telemetry/v1`，按权威 `TelemetryFrame` 解码 BMS 32 位故障字、状态、告警等级和 `alarms`；空 `message` 按 `alarm_id` 补名称，告警明细位图与故障字不一致时单独提示。页面仍记录本次会话的故障进入/清除；3 秒断流后当前值恢复“等待数据”。
- 内置整车模拟器（源码运行）发出全部代表帧，macOS 可开发整车页面；Windows 发布版不打包。
- 代码按模块拆分：后端 `canhost/decoders.py`（CAN 帧格式唯一定义）+ `bms/` + `vehicle/` + `telemetry/` 子包 + `transport.py`；前端 `web/js/` 八个模块（core/bms/vehicle/fan/bench/ivt/telemetry/updater）。
- GitHub 软件内更新已接入：Windows 发布版启动后自动检查 BITFSAE/can-host 的正式 Release，左下角版本信息打开更新窗口，支持预发布、私有仓库只读 PAT、ZIP+SHA256 下载校验、安全解压、退出备份替换失败回滚和自动启动新版本；源码运行只检查不安装。
- 界面 UI 与连接交互重构完成：左上角标题单行化（`CAN HOST`）；整车总览与整车风扇移除顶部冗余连接栏；底部把 CAN1、BMS CANB、整车 CANB 和开发模拟收成单行入口，RX/TX 合计与固件身份固定在右侧；BMS 主连接与整车连接保持独立。
- 整车模拟器 SOP 发送周期已同步为 10ms，与 F405/BMS 新协议一致。
- 实车 F405 的 SOP `0x4A0/0x4A3` 现只在放电模式已进入 `HV_ON` 后以 10ms 周期发送；上位机在自检、待机、预充、故障保持和充电模式显示“等待”属正常。帧格式和解码未变。

- 侧栏导航改为独立滚动，品牌标志和版本入口固定在底部；软件更新窗口按状态、版本对比、版本说明和高级设置分层，并在发现新版本时给版本入口增加提示。

## 待完成

### 发布前必须完成

- [ ] 在目标 Windows 10/11 和 WebView2 Runtime 上运行源码版本及PyInstaller产物。
- [ ] 发布真实 Release 后做一次软件内更新端到端验证：旧版检查到新版、下载校验、退出安装、新版自动启动，并验证安装目录无写权限/新版本启动失败时的回滚。
- [ ] 更新器 401/403、404、私密仓库令牌保存/清除、私有仓库发布版下载校验在真实 GitHub 上验证一次。
- [ ] 使用实体PCAN-USB检查连接、断开、重连、通道占用和错误提示。
- [ ] 对照PCAN-View和F405实帧检查138串、48路温度、状态、告警、继电器、预充、安全回路与IMD显示；SOP只核对后台解析和原始记录。
- [ ] 对阈值、告警开关、充电请求、电流方向、充电机类型、RTC、故障复位和Flash日志执行逐项台架验证；写入后检查周期回报和断电保存。
- [ ] 长时间记录`.bmslog`和CSV，检查高总线负载下的帧完整性、文件增长、停止记录、重新打开、暂停和拖动回放。
- [ ] 用真实 FanController 验证风扇页：`0x5AE`、V3全部命令、低压电池/DCDC两档扫频与保存、参数拒绝、租约到期和断电加载。
- [ ] 用真实F405验证电池箱风扇：`0x5AA..0x5AD`查询窗口、控制租约、高压标定安全中止、35W/70W提交、保存等待位和断电加载。
- [ ] 整车连接接实车 CANB 逐卡验证：SOP 三帧、0x4B0/0x4B1、PDM 两侧、ECU 10 ms 调试帧和胎温帧。CAN1 主监视另行验证自有 IVT 八通道，并核对 IVT U1 与 BMS 包电压一致性。
- [ ] 确认赛会能量计 0x526/0x528 在实车上是否发送（DBC 标注可能不发送）。
- [ ] 胎温帧与轮位的实际对应关系（vehicle-interfaces 标注待实物确认）确认后更新页面标签和 vehicle-interfaces。
- [ ] 使用双 PCAN 验证 BMS 主连接与整车 CANB 同机并发；同时验证 CAN1/BMS CANB 主连接切换、单路断开不影响另一服务，以及同一 PCAN 通道被占用时的错误提示。
- [ ] 用现网只读 MQTT 账号连接 `bitfsae.com:1883`，由 G473 或 `local_sim2.py` 发送正常/故障进入/切换/清除帧，验证订阅重连、故障名称、变化记录、3 秒断流和损坏 Payload 计数。
- [ ] 完成Windows发布目录检查，确认无需网络字体、开发目录或未打包模块。
- [x] 推送 `host-v*` 标签走一遍 CI：确认版本核对、单测、PyInstaller 产物和 Release 附件 zip 完整可用，手动触发一次构建验证产物下载。（2026-08-17 在原仓库已验证全流程；迁移到本仓库后标签规则改为 `v*`，需在新仓库重新验证一次手动构建。）

### 界面和操作验证

- [x] 侧栏内容增多时导航区独立滚动，品牌标志和版本入口不再被上方内容挤出；告警说明允许换行，不再用省略号隐藏诊断条件。
- [x] 软件内更新窗口重整为状态摘要、版本对比、版本说明和高级设置四层；打开窗口时刷新状态，预发布选项变化后重新检查。

- [x] JavaScript语法、HTML ID引用和PyWebView公开成员检查。
- [x] 1460×920与1120×720无水平溢出检查。
- [x] 使用模拟快照检查五页导航、同页138串/48温度、单体筛选、确认弹窗、连接弹窗和CAN表格滚动。
- [x] 长页面切换到短页面时恢复页面顶部。
- [x] 充电机类型待写入选择不被250ms周期回报覆盖。
- [x] 模拟充电模式下检查充电计时、16.2Ah预计剩余时间和双轴趋势曲线。
- [x] 电芯页删除正常状态下重复的完整性提示和全局三位编号，放大单路读数，并将模组摘要调整为极值范围与压差。
- [x] 删除模组内重复的电压/温度区标题，强化顶部统计和保护阈值层级，并启用`Ctrl++/-/0`页面缩放。
- [x] 修复Windows WebView2未响应原生`Ctrl++`的问题，改为前端明确处理主键盘、数字键盘和滚轮缩放并保存比例。
- [x] 重做故障页告警条件、等级和保护开关层级；默认缩放改为110%，并修复放大后底部内容超出可视区。
- [x] 重整高压、风扇和 IMD 的信息层级；解析 IMD Byte1 的 PB8/PWM/阻值有效/绝缘通过位，PWM 或阻值无效时不显示占位零值；高压状态帧和 IMD 帧都按新鲜度判断。
- [x] 完成版本信息和页面布局整理：底部状态栏改为统一单行，左下角合并上位机版本/日期并显示硬件 PCB V3.0（2026-05-30），不显示固件工作区是否有未提交改动；具体显示含义见 [`../../DOC/CAN上位机与工具使用.md`](../../DOC/CAN上位机与工具使用.md)。
- [x] 按现场反馈再次整理总览：状态机改为“标题在上、`SELF_TEST`/`STANDBY`/`PRECHARGE`/`HV_ON`/`FAULT`状态在下”的纵向结构并按状态着色；IMD只保留`OK_HS`、实际PWM频率、占空比和绝缘阻值，重复的有信号/有效/通过字段改为简短悬浮解释；告警开关显示DOC短名；三路继电器改为等宽状态格并移除不必要的小标题；温度/冷却与 IMD 拆成两个独立卡片；充电计时只显示已计时和预计剩余；补充状态新鲜度判断，模拟器覆盖充电阶段；写入后追踪 Flash 保存等待位并显示保存完成。
- [x] 按现场反馈压缩参数页：系统参数与谨慎操作拆为左右两个同级面板；充电请求拆分为请求值和充电机反馈；Flash 保存状态移到顶部条件栏；发布版隐藏工作模式并固定真实 PCAN，模拟器只保留源码开发路径。
- [x] 按现场反馈拆开台架模拟从控和 IVT 配置：工程工具默认折叠；删除底部连接方式选择；台架页和 IVT 页各自独立连接 CAN1/500 kbit/s，IVT 配置先读回核对，并把 Command/Response、逐通道表和过流值移到高级或展开区域。
- [x] 统一两个工具页的连接选择器、按钮和输入控件高度；注入场景改为响应式等宽网格；IVT 的完整配置明确不改位率，250 kbit/s 只保留给旧充电机过渡，当前输入目标已对齐时禁止重复配置。
- [x] 新增面向使用者的 [`../../DOC/CAN上位机与工具使用.md`](../../DOC/CAN上位机与工具使用.md)，并将本目录 README 收敛为安装、运行和发布说明。
- [ ] 在最小窗口、110%/120%缩放和 IMD 故障/无效数据状态下复核新的卡片布局，确认分卡后没有裁切、空白过大或状态值不醒目。
- [ ] 在Windows实际PyWebView窗口中复核BITFSAE标志、三线趋势图、告警开关行和连接按钮，确认模拟数据与PCAN两种模式的标签样式。
- [ ] 在实际PyWebView窗口中逐项鼠标点击和键盘操作，检查焦点、滚轮、对话框和最小窗口可读性。
- [ ] 录制一次完整充电台架操作过程，根据操作者实际使用顺序复核信息优先级和按钮位置。

## 当前风险

| 风险 | 影响 | 关闭条件 |
|---|---|---|
| 尚未完成实体PCAN和F405台架验证 | 模拟数据不能证明驱动、总线时序和真实帧完全正确 | 按 `DOC/CAN上位机与工具使用.md` 完成台架检查并保存原始记录 |
| 尚未验证Windows打包产物 | 目标电脑可能缺少WebView2、PCAN-Basic或打包资源 | 在干净目标机安装、启动并连接PCAN |
| 长时间记录性能未实测 | 高帧率运行时可能出现磁盘或界面性能问题 | 完成长时间`.bmslog`/CSV记录与回放测试 |
| 整车页各节点只有模拟数据验证 | ECU 10 ms 帧率、赛会能量计发送行为、胎温轮位、实车帧内容可能与模拟不同 | 实车 CANB 逐卡验证并记录实测帧 |
| 快捷栏高压回退逻辑只有模拟覆盖 | BMS 与整车并发时，主连接数据缺失后回退整车 CANB 镜像的取数优先级未实测 | 双 PCAN 并发时断开/恢复 BMS 主连接并核对快捷栏高压/SOC |
| 1120×720只完成浏览器渲染检查 | PyWebView缩放和Windows显示缩放可能改变实际可用区域 | 在100%、125%、150%显示缩放下检查 |
| 软件内更新未做真实 GitHub 与 Windows 端到端验证 | 真实 Release API、权限、下载速度、UAC/安装目录写权限、PowerShell 助手和新进程回滚未实机覆盖 | 用旧版升级到 v0.8.0，实测完整安装与回滚；确认安装目录权限和日志位置 |
| 私有仓库访问令牌保存在本机明文 settings.json | 使用共用电脑或电脑失窃时令牌可能被读取 | 只给发布版用户创建 `contents:read` 最小权限；丢失令牌后立即撤销并清除；公开仓库保持不配置令牌 |
| 现网 MQTT 1883 仍为明文且当前只读账号复用 telegraf | 网络路径可观察认证与 Payload；桌面用户共享服务账号不利于轮换和审计 | Broker 部署 TLS，并为上位机创建只读 `fsae/telemetry/v1` 的独立账号；TLS 实连验证后默认切换安全端口 |

## 最近验证

| 日期 | 结果 | 尚未覆盖 |
|---|---|---|
| 2026-09-03 | v0.8.0 发布前复核：底栏真实连接改为按保存参数一键连接/再次点击断开，模拟入口统一启停两套开发数据源；修复整车 RX/TX 合计、快捷栏高压/电流/SOC 独立回退、CAN 监视器可用源自动选择、风扇占空比单位和标定进度状态。143 项单测、Python 编译、全部 JS 语法和差异格式检查通过 | Windows PyWebView、双 PCAN 并发、实体 F405/FanController 与软件内更新仍待验证 |
| 2026-09-01 | 发布 0.6.0 前复核：对照 BMS-MASTER-F405 `App/bms_can_protocol.c` 与 `DOC/CAN通信协议.md` 逐项核对协议版本 5、故障帧版本 4、告警开关帧版本与 23 个开关位、阈值/充电参数范围和强制位掩码，上位机与固件一致；固件 9d9123d 仅调整保护策略文档与强制策略注释，无帧格式变化。132 项单测、Python 编译和全部 JS `node --check` 通过；使用文档工具命令版本改为 5 | 实体 PCAN 与 F405 联调仍待验证 |
| 2026-09-01 | 同步 F405 告警开关 v5：`build_command` 默认整组全部开启，允许调用方关闭任意动作位；模拟器接受任意合法开关，`test_bms_protocol` 54 项、全套 132 项和 `node --check` 通过 | 实体 PCAN 在 Debug-Bringup 关闭第一类/第二类开关、正式固件拒绝关闭强制位联调 |
| 2026-09-01 | 从控原始电压/温度按通道解码：`0xFFFF`/`0xFF` 只把该格标成断线并显示「断」，1 V / 6.5 V 等模拟量保留数值；同帧其他串不再被整帧作废。`test_bms_protocol`/`test_decoders` 65 项通过，`node --check` 通过 | 实体 PCAN 拔 23 负、20/21 虚接与 F405 Debug-Bringup 联调 |
| 2026-08-31 | 修复软件内更新后新窗口不弹出的问题：安装助手改用 `ProcessStartInfo` + `UseShellExecute` 启动新进程，缓解隐藏 PowerShell 会话继承导致的无窗口；并改为检查新进程存活约 8 秒后再判定成功，旧版本备份不再立即删除，保留最近一份 `BITFSAE_CAN_Host.old-*` 供手动回退，更早备份自动清理。更新器测试和全部单测通过，版本升至 0.5.1 | 真实 Windows 发布包上的更新/回滚和窗口弹出仍待实测 |
| 2026-08-31 | 最终提交 IVT CAN1 迁移与上位机过压上限联动：上位机 IVT 配置改独立 CAN1/500kbit，清除整车页旧 IVT 显示，补齐 pcan_ivt_tool 的 bms-can1 命令与边界文档；Debug-Bringup 允许写到 4500mV，正式固件由 F405 拒绝。裸测 127 项全绿、JS/HTML/AST 检查通过，版本升至 0.5.0 | 真实 IVT 250kbit 与 CAN1 共线负载、Windows 发布包和软件内更新仍待验证 |
| 2026-08-30 | IVT 页面新增八通道周期直接编辑、载入读回值和合计帧率显示；自定义值贯通读取目标比较、完整配置、重启读回，默认 I 20 ms、其余 100 ms；同日把自有 IVT `0x512..0x519` 的主监视、配置、模拟和测试从 CANB 迁到 CAN1。隔离依赖环境下 127 项完整单测、全部 JS 语法、Python 编译和差异格式检查通过；版本升至 0.4.1 | 真实 IVT 自定义周期写入、重启读回、F405 360 ms 新鲜度、CAN1 从控与 IVT 共线负载待实车验证 |
| 2026-08-30 | 侧栏固定底部品牌/版本区，更新窗口分层并增加可用更新提示，告警条件改为可换行；相关 JS 语法、HTML 重复 ID、CSS 括号和差异格式检查通过 | 实际 PyWebView/WebView2 中的最小窗口、缩放和鼠标交互仍待复核 |
| 2026-08-29 | MQTT 遥测订阅与故障解析接入：权威 Proto/生成文件哈希核对一致；121 项单测、全部 JS `node --check`、Python 编译、HTML 重复 ID 和 `git diff --check` 通过 | 真实 Broker 认证/重连、现网 G473 Payload、Windows PyInstaller 对 paho-mqtt/protobuf 的实际收集和 PyWebView 页面操作仍待验证 |
| 2026-08-27 | GitHub 软件内更新实现：后端检查/下载/SHA256/安全解压/备份替换回滚，前端更新窗口；CI Release 同时上传 ZIP 与 .sha256；102 项单测、全部 JS `node --check`、Python 编译和 `git diff --check` 通过 | 真实 GitHub Release 请求、Windows PyInstaller/WebView2 下的更新按钮与安装助手端到端、安装目录权限/UAC 回滚 |
| 2026-08-26 | Bringup 从控监视窗口统一为 350ms，AMK 干扰改由 HV_ON 动作开关处理 | 实体 PCAN 与 F405 Bringup 联调仍待验证 |
| 2026-08-24 | 仓库迁移与整车扩展：filter-repo 保留 29 条 host 提交历史迁入本仓库并推送 GitHub（私有，Electrical-core 团队写权限）；后端拆分 decoders/bms/vehicle/transport，前端拆分六个 JS 模块；新增整车总览页、快捷栏、监视器数据源切换；86 项单测全绿、全部 JS `node --check` 通过、模拟模式 Api 冒烟通过 | 实车 CANB 各节点、双 PCAN 并发、Windows 打包（新仓库 CI 待手动触发验证） |
| 2026-08-20 | Windows exe 更换默认图标为 BITFSAE 鲨鱼标：`app_icon.ico`（16–256 px 七档，≤128 px 为 BMP 帧、256 px 为 PNG 帧，均从 `web/assets/shark-mark.svg` 矢量渲染），spec 的 `EXE()` 加 `icon=`；ico 结构与各尺寸透明度经 Pillow 读回校验 | Windows 实际构建后的 exe/任务栏图标显示效果待发布时确认；旧安装位置需图标缓存刷新 |
| 2026-08-20 | 修复悬浮提示（单体“最近数据 X s”等）时有时无的问题：单体网格、保护开关状态表和 CAN 监视器行改为保留 DOM 节点、只原地更新内容，轮询不再销毁鼠标下的元素；59 项主机测试和 `node --check` 通过 | DOM 复用逻辑靠代码审查覆盖，无自动化 UI 测试；实体 PCAN 实机悬浮效果待台架复核 |
| 2026-08-20 | 修复 PCAN 接收时间戳按适配器计数导致故障码变化记录等处显示 1970 的问题，接收帧统一用上位机本机时间打戳；RTC 校时显示改为本次连接内持久显示最近应答，未校时时显示主控 RTC 有效性位；59 项主机测试（含 IVT）和 `node --check` 通过 | 实体 PCAN 实机显示效果待台架复核；旧记录文件时间无法恢复 |
| 2026-08-17 | 整车风扇工具页：风扇协议测试已提交进 `Tests/test_bms_host_protocol.py`（`FanControllerToolTest`，与兄弟仓库 `Doc/风扇控制.md` 对照）——`build_fan_command` 7 组文档示例帧逐字节一致（含 CRC）、`0x5A2/0x5A3/0x5A5/0x5A6/0x5A7` 解码、16 组非法参数拒绝、扩展帧隔离、发送前置检查、主快照不含风扇数据；58 项主机测试全部通过，`node --check` 和 Python 编译检查通过 | 真实 FanController 实帧、实体 PCAN 和 Windows 发布环境仍待验证 |
| 2026-08-04 | 50 项主机测试、Python/JavaScript 语法、HTML/CSS结构和差异格式检查通过；BMS 主监视、台架模拟从控、IVT 配置已拆为独立连接，使用文档已移入 DOC | 实体双 PCAN、CANB 满载、真实 IVT、ECU/数采接收和 Windows 发布环境仍待验证 |
| 2026-08-03 | 29 项主机协议测试、固件三套构建、Python/JavaScript 语法和差异格式检查通过 | 实体 PCAN、CANB 满载、真实 IVT、ECU/数采接收和 Windows 发布环境仍待验证 |
| 2026-08-03 | 27 项主机协议测试通过，Python 语法和差异格式检查通过 | 实体 PCAN、CANB 满载、真实 IVT、ECU/数采接收和 Windows 发布环境仍待验证 |
| 2026-08-03 | 日期边界测试通过 | Windows 实际 PCAN-Basic、WebView2、one-folder 产物和 F405 台架仍待完成 |
| 2026-08-03 | 24 项主机测试、Python/JavaScript 语法、HTML ID 和差异格式检查通过 | Windows 实际 PCAN-Basic、WebView2、one-folder 产物和 1120×720/110%/120% 视觉复测仍待完成 |
| 2026-08-03 | 20 项主机测试、Python/JavaScript 语法检查通过 | 实际 PyWebView/WebView2 缩放、状态颜色、悬浮提示和 F405 外置 Flash 保存仍待现场复核 |
| 2026-08-03 | JavaScript 语法、CSS 括号、HTML ID 和 `git diff --check` 通过 | 实际 PyWebView/WebView2 缩放和故障态视觉复测仍待验证 |
| 2026-08-03 | 20 项主机测试、Python/JavaScript 语法、HTML ID 和差异格式检查通过；Chromium 模拟数据截图和 1120×720 静态截图未见水平溢出 | 原生 PyWebView/WebView2 的 110%/120% 缩放、IMD 故障状态和实体 F405 实帧仍待验证 |
| 2026-08-03 | 18 项主机测试、JavaScript 语法和差异格式检查通过 | Windows 实际 PyWebView、实体 PCAN、F405 安回/IMD 实帧和台架视觉复测仍待完成 |
| 2026-08-03 | 协议测试、JavaScript 语法、CMake 版本头生成和差异格式检查通过 | 实际 PyWebView 窗口的底部布局、按钮点击、模拟数据交互、实体 PCAN、F405 台架、Windows 打包和长时间记录仍待验证 |
| 2026-08-03 | macOS 源码运行验证通过（pywebview 6.2.1、python-can 4.6.1），原生窗口打开和退出无报错；16 项协议测试、JavaScript 语法检查通过 | macOS 无实体 PCAN 驱动，模拟数据交互、文件对话框和缩放快捷键待实际点击复测 |
| 2026-08-03 | 17 项测试、JavaScript/Python 语法、HTML ID 和差异格式检查通过 | 实际 PyWebView 鼠标/键盘复测、实体 PCAN、F405 台架、Windows 打包和长时间记录 |
| 2026-08-02 | 16 项主机协议测试、PyWebView API 不公开检查、JavaScript 语法、HTML ID 引用、`node --check` 和 `git diff --check` 通过；Chromium 模拟快照（五页导航、确认/工程弹窗、CAN 列表滚动）无脚本错误，1460×920 与 1120×720 无水平溢出；总览信息、同页 138 串/48 温度、只读故障页、独立命令页、异常筛选、充电计时、预计时间、趋势曲线和统一字号通过 | 修正后的 PyWebView 原生窗口复测、实体 PCAN、F405 台架、Windows 打包和长时间记录 |

## 已完成变更

### 2026-09-03

#### 底栏单行连接与独立服务状态修正

- 底栏压缩为一行：左侧依次为 CAN1、BMS CANB、整车 CANB 和仅源码开发态显示的模拟按钮；删除无明确对象的“…”按钮和重复的全局“未连接”文字；RX、TX 与固件身份移到同一行右侧。
- CAN1/BMS CANB 只在 BMS 主服务内互斥，整车 CANB 保持独立，可使用另一 PCAN 通道并行连接；断开任一服务不再连带断开另一服务。
- 三个真实连接按钮均按本机保存的通道/位率一键连接、再次点击断开；低频参数收进底栏最右侧的统一连接设置，CAN1 固定 500 kbit/s。模拟按钮统一启动/停止 BMS 与整车开发模拟源，并拒绝和真实 CAN 混用。
- 删除运行总览顶部重复显示“CAN1 · BMS 主监视”的作用域条，连接状态只由底栏按钮表达。
- RX/TX 改为 BMS 与整车服务的合计值，悬浮显示分路计数；固件未收到有效身份帧时明确显示“等待数据”。

### 2026-09-02

#### 底部三角色快速连接坞与风扇页统一

- 去掉本轮上一版总览和整车页的空态引导条（底部已有连接入口），避免重复提示。
- 底部状态栏改为 CAN1、BMS CANB、整车 CANB 三个角色按钮；本版曾把三者错误做成全局单连接，已在 2026-09-03 修正为“BMS 主服务内部互斥、整车服务独立”。
- CAN1 固定为 BMS 主监视（可写）；BMS CANB 走主 BMS 协议（0x4B0/0x4B1 镜像，只读，无 138 串）；整车 CANB 走车辆协议（SOP/PDM/ECU/风扇命令）。
- 通道与位率在 2026-09-03 收进统一连接设置；开发用“模拟”按钮单独存放，发布版自动隐藏。
- 原 BMS/整车连接弹窗在 2026-09-03 合并为低频设置弹窗，同时删除全局角色短状态并恢复两项连接独立显示。
- 增加页内作用域提示：总览、CAN 监视器、参数与命令、整车风扇分别显示当前连接的实际用途；参数页在非 CAN1 时按钮降饱和但仍保留可点击（提交时后端继续拒绝），风扇页在非整车角色时明确提示需整车遥测。
- 整车风扇页统一子块标题、面板表面、间距和按钮节奏；电池箱风扇增加“当前实际占空比 / 保存状态”独立状态值，取消重复的状态拼接；子块分段改用统一“标题 + 说明”结构。
- 验证：全部 JS 语法检查、142 项测试、CSS 变量/HTML 重复 ID 和 git diff 检查通过；1460×920 与 1120×720 底部连接坞保持一行且无水平溢出，风扇页真实两列布局已截图检查。真实 PyWebView 点击连接/断开、角色切换和实体 PCAN 仍待现场验证。

#### 界面 UI 第二轮优化（连接空态、异常可读性与卡片层级）

- 运行总览与整车总览增加未连接引导条：未连接时明确给出“连接 BMS 主监视 / 连接整车 CANB”入口，任一相关连接建立后自动收起；引导文案只说明连接关系，不重复协议细节。
- 移除工作区里与“状态机信息条不添加彩色左侧竖条”规范冲突的状态条彩色边和阴影；状态机恢复普通卡片样式，仍通过状态文本和下方卡片颜色表达。
- 电芯与温度页每路异常格增加文字标注（欠压/过压/低温/过温/失效·缺失），不再只靠颜色识别；每个 BMU 标题栏增加“在线/部分缺失/离线”徽标，缺失数量和通信中断保留原提示。
- 整车风扇自动阶梯扫频增加进度条与步骤/百分比文案：正在扫描时显示当前步骤和完成度，中止时显示原因，完成时提示核对推荐上限；配合原有标签状态和表格使用。
- 确认弹窗顶部增加操作类型徽标：BMS 工具命令、CANB 风扇命令/动作、IVT 配置操作、危险操作分开显示；告警开关整组写入按危险操作显示，避免只看到“确认发送”而忽略风险。
- 统一面板/数据卡片表面层级：补齐边线、卡片顶部内高光与确定性的 hover 反馈；弹窗补深色 color-scheme；底部快捷栏常看值增加轻微上投影。
- 台架模拟从控、IVT 配置页头的工程工具栏从“仅下边框”改为与页面主卡片一致的圆角面板，减少页面间视觉跳跃。
- 验证：node 语法检查全部 JS 通过；CSS 变量引用、HTML 重复 ID 和 git diff 检查通过；1460×920 与 1120×720 无水平溢出，空态按钮与底部快捷栏在窄窗口可读。

#### 风扇标定交互与安全校验修正

- 整车风扇标定页面按所选档位自动设置总线电流保护：低压电池档 8A、DCDC 档 18A，页面加载时同步一次，避免默认停留在另一档位的保护值。
- 保存整车风扇两档上限前增加“5% ≤ 电池档 ≤ DCDC档 ≤ 100%”校验：页面直接提示，后端 `build_fan_command` 同步拒绝，避免发送后才被 FanController 判为参数错误。
- 整车风扇标定完成后只在对应档位有推荐值时填入；缺少另一档时状态区明确显示“电池待标定/DCDC待标定”，不再让人误以为两档都已完成。
- 修正电池档扫频的重复采样路径未传递所选供电档位到看门狗的问题；该路径在数据波动重试时可能误按 DCDC 状态放行，现与正常采样路径一致。

#### 整车风扇推荐上限修复

- 整车风扇自动扫频完成后的推荐上限改用扫频记录的总线总电流（`current_a`）计算：电池档按 8.0A、DCDC 档按 18.0A 选择最大安全占空比，并要求被扫回路转速正常；不再误用 `delta_current_a` 导致超出含背景负载的总预算。
- 同一档位重复扫不同回路时保留更保守的推荐值；没有可用数据时不生成 15% 的假建议。
- 修复页面 `renderFan()` 中推荐上限区域在会话状态变量声明前读取导致的运行时错误，并保持用户正在编辑的输入框不被覆盖。

#### MQTT 遥测记住密码

- 遥测订阅设置的密码随 Broker、端口、Topic、用户名和 TLS 选项一起保存在本机页面设置（浏览器本地存储）中，打开订阅设置弹窗时自动填入，不需要每次连接重新输入；连接成功、断开后仍清空输入框，下次打开弹窗时从保存的设置恢复。
- 保存动作从页面加载时移到每次打开弹窗时执行，保证弹窗始终显示最近一次成功订阅使用的连接参数。
- 密码明文只保存在本机 WebView 存储中，不在仓库、日志或后端状态快照中出现；后端 `telemetry/service.py` 行为不变，密码仍只在建立连接时使用。共用电脑上不要保存密码。
- 同步更新 `DOC/CAN上位机与工具使用.md` 的安全边界和遥测连接两处说明。
- `fsae_telemetry_pb2.py` 按 protobuf 7.36 生成，依赖下限同步提高到 `>=7.36,<8`，避免运行时版本校验不兼容。

### 2026-09-01

#### 两套风扇控制和分档标定

- FanController协议升为V3：新增`0x5AE`，上位机可分别在低压电池/DCDC档执行扫频，导出基线和逐点数据，并用Action 5/6保存或清除两档上限；未标定值显示15%。
- 新增F405电池箱风扇`0x5AA..0x5AD`编解码、命令应答跟踪和页面操作；CAN1主监视同步解析`0x186E50F4`及`0x186250F4`新增状态位。支持手动标定步骤、35W/70W上限提交和Flash保存状态复核。

#### Debug-Bringup 从控监视窗口 600ms

- 收到固件身份帧为 Debug-Bringup（`0x186C50F4` Byte1 bit1..0=2）时，从控电压/温度页和模组在线判断使用 600ms 新鲜窗口；正式 Debug/Release 仍为 350ms。对应 `BmsProtocol.slave_sample_timeout_s` 按身份刷新，测试同步。

#### 告警开关新增采样线断线两位（布局版本 0x06）

- 固件告警开关位图新增电压/温度采样线断线开关（Byte2 bit2/bit1），`SWITCH_DEFS`、命令默认掩码（Byte2 `0xF8`→`0xFE`）、开关页确认文案和 `pcan_bms_bench.py` 开关解码同步；开关帧版本字节改用独立的 0x06 布局版本，命令信封协议版本仍为 5。模拟器和测试同步。

#### 告警开关 v5（动作开关）与版本 0.6.0

- `TOOL_PROTOCOL_VERSION` 升为 5，与固件 `BMS_CAN_TOOL_PROTOCOL_VERSION` 一致；统一应答 Byte5 bit5（当前构建允许配置 HV_ON 动作开关）已解析。
- 告警开关改为整组 23 位写入，默认全部开启；正式 Debug/Release 固件拒绝关闭强制位并返回参数无效，Debug-Bringup 允许关闭第一类和第二类动作位。模拟器和测试同步。
- 参数与命令页说明改为「关闭后仍上报」：关闭开关只停止保护动作，原始检测、故障码和等级明细继续上报。

#### 从控原始电压/温度按通道显示

- `_decode_cells` 不再用 `all(500..5000)` / `all(<=129)` 把整帧作废。`0xFFFF`/`0xFF` 只让对应格子状态为「断线」、显示「断」；同帧其他串保留数值。抽头虚接的 1000 mV / 6500 mV 按数字显示，由主控阈值标成欠压/过压。
- 仅 DLC 不足仍整帧失败。温度单路 >129 只该路「范围错误」。
- 与 F405 主控第 4 节接收规则对齐。电芯页图例改为「断线 / 过期 / 缺失」。

### 2026-08-29

- 新增独立 MQTT 遥测订阅服务 `canhost/telemetry/service.py`：Paho MQTT v2 回调、异步连接/自动重连、只允许明确 Topic、无发布接口、密码不进入快照；统计接收量、解析错误和最新数据年龄，并保留 120 条会话内故障变化。
- 同步遥测仓库生成的 `fsae_telemetry_pb2.py`（权威 Proto 与 `vehicle-interfaces/telemetry/fsae_telemetry.proto` SHA256 一致），`protocol.py` 优先解析 `battery_fault_code`、兼容旧 `fault_code`、按 F405 同一 32 位名称表展开，并独立解析 BMS 状态/告警等级和 `TelemetryFrame.alarms`。
- 新增“遥测故障”页面及连接弹窗：显示连接、接收/失败计数、当前故障、活动位、遥测 Alarm 和变化记录；数据超过 3 秒明确显示“等待数据”，BMS presence 缺失时不把零值显示成正常。Broker/Topic/用户名可本机记忆，密码不保存；MQTT 与四类 CAN 连接互不影响。
- `requirements.txt`、PyInstaller hidden import、API、使用说明、文档索引和测试同步接入；版本日期更新为 2026-08-29。

### 2026-08-27

- 新增 GitHub 软件内更新 `canhost/updater.py`：标准库实现，检查 `BITFSAE/can-host` Release（默认正式版，可含预发布），下载 ZIP 与对应 `.sha256`，校验 SHA256，拒绝 ZIP 中绝对路径、`..` 和符号链接后才解压；只读 GitHub PAT 按用户保存在 `%APPDATA%\BITFSAE\CAN Host\settings.json`，可用于私有仓库，不返回前端。
- Windows 发布版退出安装：`launch_installer` 生成隐藏 PowerShell 助手，等待旧进程最多 90 秒，旧目录备份为 `.old-<时间戳>`，移入新目录、启动新 exe；新进程启动失败自动恢复旧目录，成功后再清理备份和临时目录。源码运行不支持替换安装目录。
- `canhost/app.py` 接入更新状态/检查/自动检查/下载/安装/令牌 API，bootstrap 暴露版本仓库、安装支持和设置路径；安装成功后延迟 1.5 秒关闭 PyWebView，保证 JavaScript 调用先收到结果。`canhost/web/js/updater.js`、更新弹窗和样式接入左下角版本入口。
- `.github/workflows/release.yml` 在压缩 ZIP 后生成 SHA256，并将 ZIP 与 `.sha256` 一并上传和附加到 Release；版本升至 0.3.0。
- `Tests/test_updater.py` 覆盖版本比较、资产匹配、校验文件解析、ZIP 安全解压、后台检查/下载/失败状态、私有仓库 401/403 提示、令牌持久化和源码禁装；同步 README、DOC 使用文档和 DOC 索引。

### 2026-08-26

- Debug-Bringup 从控逐帧新鲜度窗口与 F405 统一为 350ms；AMK 干扰通过四个 HV_ON 动作开关处理，不延长原始数据有效窗口。

### 2026-08-25

- 修复源码运行下“连接 BMS 模拟数据后页面仍像未启动”的前端问题：`waitForApi` 必须等 PyWebView 方法真正生成后再返回，不能只判断 `pywebview.api` 对象存在；主 BMS 快照渲染也不再被整车/快捷栏等可选快照的偶发失败阻塞。
- 修复 `renderConnection` 在 macOS/真实 PyWebView 中抛出的 `querySelector` 语法错误：`setClass` 原先只接受选择器字符串，但车辆状态胶囊传入了 DOM 元素；现在同时兼容字符串和元素。
- 修复底部“整车连接”按钮：现在点击会打开专属 `vehicleConnectDialog`；同时恢复 CAN 监视器方向筛选按钮的绑定。
- 修复整车高低压趋势卡片无限变高的问题：`#vehicleTrendCanvas` 之前没有固定 CSS 高度，JS 每次按 `clientHeight` 重设 canvas 属性后，CSS `height:auto` 又按新宽高比回写，导致画布越来越高；现在与总览趋势一致固定为 238px。
- 品牌左上角标题统一为 `CAN HOST`（全大写），同步页面测试与进度文档。

### 2026-08-24

#### 从 BMS_MASTER_F405 迁出为独立仓库

- `git filter-repo` 保留 `TOOLS/bms_host`、两个 PCAN CLI、上位机测试、CI 工作流和使用文档的 29 条提交历史，迁入本仓库（BITFSAE/can-host，私有，Electrical-core 团队写权限），内容与原仓库 HEAD 逐文件核对一致。
- 目录重排：`TOOLS/bms_host` → `canhost` 包（`python -m canhost`），CLI 脚本移入 `cli/`，requirements/build/spec/图标/todo 提升到仓库根；应用更名 BITFSAE CAN HOST，exe `BITFSAE_CAN_Host`，CI 标签规则 `host-v*` → `v*` 并保留 workflow_dispatch。
- 固件仓库删除迁移内容并按 FanController 模式登记兄弟仓库指引。

#### 整车 CANB 总览与快捷栏

- 后端：`decoders.py` 集中全部帧格式定义（SOP 小端配对 CRC、包状态、故障、告警等级、自有 IVT 小端、赛会能量计大端、PDM 大端与四字段离线判定、风扇编解码、ECU 四轮小端 + 状态半字节、胎温整数+小数）；`vehicle/protocol.py` 整车状态机（各模块独立新鲜度、风扇命令应答跟踪）；`vehicle/simulator.py` 全节点模拟源；`transport.py` CanService 按协议类型参数化，BMS 命令门控逐字保留并加协议类型守卫。
- 前端：`web/js/` 按 core/bms/vehicle/fan/bench/ivt 拆分，无构建步骤不变；导航分组 BMS（Alt+1-4）/整车（Alt+5-6）/监视（Alt+7）/工程工具（Alt+8-9）；整车总览页八类卡片 + 高低压双轴趋势；风扇页并入整车连接；状态栏加整车连接胶囊和九格常看值快捷条；CAN 监视器加数据源切换。
#### 界面 UI 与连接交互重构优化

- 左侧栏标题单行化：将左上角分行的 `CAN` / `HOST` 改为与银鲨图标水平对齐的单行 `CAN HOST`，更新 `.brand-title` 样式。
- 整车总览与整车风扇去冗：移除页面顶部重复占空间的整车连接栏（`.tool-header-bar`），释放垂直高度给整车各节点卡片与风扇遥测，使整车页面与 BMS 监视页面保持统一的紧凑顶格布局。
- 底部状态栏连接逻辑理顺：
  - 底部状态栏提供两个对等、语义清晰的连接入口：`BMS 主连接`（CAN1 / CANB / 模拟）与 `整车连接`（CANB / 模拟）；
  - 新增专属的“整车 CANB 连接”弹窗（`#vehicleConnectDialog`），支持选择 PCAN 通道、位速率（500k / 250k / 内置模拟数据）、连接与断开；
  - 修复整车连接弹窗在未连接时默认选中项以及模拟模式下位速率匹配错误，确保点击模拟数据可直接启动整车 CANB 仿真；
  - 消除“连接上位机”歧义名称，BMS 与整车连接按钮具备一致的状态指示点（绿/黄/灰）与实时通道参数展示。
  - 修复多 script 直载模式下的全局作用域绑定（`window.$` / `window.state`）与 `waitForApi` 在事件提前派发时的轮询保底，确保 BMS 连接后立即触发 `poll()` 与全量页面刷新。
- 组件间距与布局细节优化：
  - 重构 `.vehicle-flag-list` 与 `.vehicle-ecu-ack`：废弃简陋的虚线底边和狭窄间隙，改为符合工业遥测风格的独立圆角背景卡片网格（`var(--surface-2)`），解决两列文字紧贴碰撞的问题；
  - `ECU 调试帧` 表头数字列（扭矩/转速/电机/逆变器/IGBT）右对齐对齐下方读数；
  - `SOP 限功率`（3列）、`BMS 镜像`（2列）、`ECU 汇总`（3列）各自自适应列数，彻底消除空缺与溢出。

### 2026-08-20

#### Windows exe 图标

- 打包产物 `BITFSAE_BMS_Control_Desk.exe` 之前是 PyInstaller 默认图标，原因：spec 的 `EXE()` 未传 `icon=`。现在 `bms_control_desk.spec` 指定 `icon=app_icon.ico`，`build_windows.ps1` 和 GitHub Actions 发布流程走同一 spec，无需额外改动。
- `app_icon.ico` 由 `web/assets/shark-mark.svg`（天蓝 `#0EA5E9` 单色、透明背景，深浅任务栏均可辨识）用 cairosvg 按尺寸逐档矢量渲染生成：16/24/32/48/64/128 px 为 BMP（DIB）帧、256 px 为 PNG 帧。小尺寸帧不用 PNG 是因为 Windows 只官方保证 256 px 帧的 PNG 压缩，BMP 小帧在旧环境兼容性最好。文件已提交进仓库，构建时直接读取，不需要构建机装 SVG 工具。
- 替换已安装目录里的旧 exe 后图标仍显示旧的，属于 Windows 图标缓存：把 exe 改名或换目录即可看到新图标，或运行 `ie4uinit.exe -show` 刷新缓存。
- 验证：Pillow 读回 ico 确认 7 帧、帧类型正确、各尺寸四角透明、鲨鱼标占位 20%–29%。Windows 实机构建后的显示效果待正式发布时确认。

#### 悬浮提示稳定性修复

- 修复鼠标悬浮在单体电压/温度格上看“状态 · 最近数据 X s”时提示时有时无的问题。根因：每 250/500 ms 轮询后前端用 `innerHTML` 整块重建单体网格，鼠标下的元素被销毁重建，原生 title 提示的悬停计时被反复清零，弹出与否取决于悬停时机和轮询周期的竞态。
- 单体页改为首次进入时建好 6 模组 × 23 电压 + 8 温度的固定网格，之后每轮只原地更新数值、状态样式和 title；CANB 无单体原始数据的占位符也只在状态切换时写入一次。悬浮提示随轮询刷新数据年龄（保留 2 位小数），不再消失。
- 保护开关状态表（参数与命令页）同样改为启动时建一次、之后只改状态文字和样式，悬浮的名称/代码/变量提示保持稳定。
- CAN 监视器行按整行内容做签名复用：连续两轮内容完全一致时不做任何 DOM 操作，静止或暂停回放时悬浮查看完整数据/帧名稳定；有新帧时旧行节点保留并复用，只把新行插到顶部。筛选条件（方向/搜索）变化时重建行集，属于预期行为。
- 验证：59 项主机测试和 `node --check` 通过。DOM 复用逻辑经代码审查确认：固定 6×23/6×8 网格与快照固定 138/48 项数据一致（`protocol.py` 快照恒定输出该长度）、`onlyAbnormal` 语义与旧版一致（缺失值 `cellStatus` 返回 `invalid` 计入异常）、CANB 切换后网格重置重建、帧行池静止/暂停时零 DOM 操作。无自动化 UI 测试，实机悬浮效果待台架复核。

#### 接收时间戳与 RTC 校时显示修正

- 修复故障码变化记录、CAN 监视器时间列和 CSV/`.bmslog` 回放显示 1970 的问题。根因：python-can 的 PCAN 帧时间戳在环境缺少可选 `uptime` 库时是“适配器/系统启动以来的计数”，不是 Unix 时间；接收线程改为用收到帧时的上位机本机时间打戳（`CanService._frame_from_message`），历史记录、录制文件和回放显示统一走同一本机时钟。修改前录制的旧 CSV/`.bmslog` 文件内时间已经是错的，回放旧文件时无法恢复，以重新录制为准。
- RTC 校时按钮旁的状态不再在 5 秒后回到“等待新回复”：本次连接内持续显示最近一次校时应答和主控读回的时间；尚未校时时按主控周期上报的 RTC 有效性位（`0x186B50F4` bit7）显示“主控 RTC 有效”或“RTC 未校时”，收不到运行诊断帧时显示“等待数据”。
- 新增接收帧本机时间打戳用例和 RTC 应答持久性用例；`Tests/test_bms_host_protocol.py` 50 项全部通过，`node --check` 通过。

#### 从控未就绪和离线显示拆分

- 故障 bit16 显示“从控数据未就绪”；故障帧 Byte6 bit0..5 只显示超时窗口内完全收不到报文的从控离线状态，与固件新的在线/就绪判断一致。
- 故障帧格式版本字节随固件升为 0x03；模拟器和测试数据中的故障帧版本字节同步，上位机本身不校验该字节。

### 2026-08-18

#### Windows 11 字体回退修正

- Win11 实机反馈界面字体有回退、部分不易读，定位到 `web/styles.css` 字体栈并修正：
- `--ui` 去掉首位的 Bahnschrift（DIN 风格窄体，小字号可读性差），改为 `Segoe UI Variable Text`（Win11）→ `Segoe UI`（Win10）→ `-apple-system`（macOS 开发机），中文继续落到微软雅黑 UI / 苹方。
- `--mono` 原来没有任何中文字体，CAN 监视器表格（`td` 全走等宽栈）里的中文帧名在 Win11 上落到通用等宽兜底，常显示为宋体或日文字形；现在等宽栈末尾补 `Microsoft YaHei UI` 和 `PingFang SC`，中文在等宽上下文里用雅黑渲染。
- `--cjk` 补 `Microsoft YaHei` 二级回退；`__version_date__` 更新为 2026-08-18 并发布 `host-v0.1.0-rc2`。

#### CI 预发布支持与首个预发布

- `host-release.yml` 支持预发布标签：`host-vX.Y.Z-rc1` 这类版本号后带后缀的标签创建为 GitHub 预发布（Pre-release），版本号主体仍与 `__version__` 核对；不带后缀仍是正式 Release。`README.md` CI 一节同步说明。
- 推送 `host-v0.1.0-rc1` 发布首个预发布版本供下载试用；「发布前必须完成」清单（实机 Windows、真实 PCAN、FanController 实车验证）完成后再打正式标签。

### 2026-08-17

#### GitHub CI 构建与发布

- 新增 `.github/workflows/host-tests.yml`：main 推送和 PR 涉及上位机代码或上位机测试时，在 windows-latest + Python 3.11 上运行 `Tests/test_bms_host*.py` 单元测试。
- 新增 `.github/workflows/host-release.yml`：推送 `host-v*` 标签触发，先核对标签版本与 `TOOLS/bms_host/__init__.py` 的 `__version__` 一致（不一致直接失败），再复用 `build_windows.ps1` 在 windows-latest 完成测试和 PyInstaller 打包，将 one-folder 目录压缩为 `BITFSAE_BMS_Control_Desk_<标签>.zip` 并创建 GitHub Release 附上附件；手动触发时只上传构建产物，不创建 Release。
- `README.md` 新增“CI 自动构建与发布”一节，说明标签约定和发布步骤。
- CI 首跑发现 Windows 编码问题并修复：`test_overview_markup_matches_information_hierarchy` 读取 `web/index.html` 未指定编码，Windows 默认 cp1252 解码 UTF-8 中文直接报错（英文 Windows 上 `build_windows.ps1` 同样会挂）；改为 `read_text(encoding="utf-8")`。应用本体经 `file://` 由 WebView2 渲染界面，不受影响。
- CI 首跑发现 `build_windows.ps1` 吞掉失败并修复：Windows PowerShell 对原生命令非零退出码默认不中止，单测失败后仍继续 PyInstaller 打包并以退出码 0 结束；现在 venv 创建、pip、单测和 PyInstaller 每步检查 `$LASTEXITCODE`，失败立即退出，测试不过不再出包。

#### 整车风扇工具页

- 新增独立“整车风扇”工程工具页（Alt+8）：独立 PCAN 连接整车 CANB（500/250 kbit/s），与主监视、台架和 IVT 互不影响。
- `protocol.py` 解析 FanController 报文：`0x5A2` 三路转速与两路实际占空比、`0x5A3` 故障位/模式/温度输入有效位/最高电机与控制器温度/目标占空比、`0x5A5` 命令应答、`0x5A6` 自动曲线回报、`0x5A7` 失联策略回报；`0x7FFF` 温度无效值不显示为读数；`0x506..0x508/0x5A2..0x5A7` 加入帧名表，CAN 监视器和记录文件可直接显示名称。
- `build_fan_command` 覆盖五种操作码并按 FanController 固件规则逐项校验（模式、占空比 0..100、有效时间 1..60 秒、关闭模式占空比必须为 0、关闭<启动<全速≤150 ℃、保底 0..100%、保持 0..30 秒、升降速 10..100%/s、恢复默认键 `0xA5`）；CRC-8/SAE-J1850 与固件一致。
- `send_fan_command` 只允许真实 PCAN + CANB 发送，带独立序号，等待 1 秒内操作码和序号都匹配的 `0x5A5` 应答；租期到期补发的应答不会误匹配为新命令结果。
- 优化整车风扇页面 UI 布局与交互层级：按动力总成回路整合遥测（PWM1 水冷回路驱动风扇 1/2，PWM2 风冷/电驱回路驱动风扇 3），转速表卡片集成运行/停转/停机动态状态；精简冗余文字与协议长段说明，统一输入单位后缀与操作按钮；自检故障矩阵在全正常时保持静默并在异常时高亮告警。
- 使用说明新增 `DOC/CAN上位机与工具使用.md` 第 6 节。

#### 审查修正

- 对照兄弟仓库 FanController `Doc/风扇控制.md` 与 `Core/Src/fan_controller.c` 逐项核对帧格式、参数范围（租期 1..60 s、保持 0..30 s、升降速 10..100%/s、恢复键 `0xA5`）和 CRC-8/SAE-J1850，实现与固件一致；外部仓库位置已登记进根目录 `AGENTS.md`。
- 风扇协议测试提交进 `Tests/test_bms_host_protocol.py`（`FanControllerToolTest`）：7 组文档示例帧逐字节比对、状态/诊断/应答/曲线/失联策略解码、16 组非法参数拒绝、扩展帧隔离、发送前置检查和主快照隔离。
- 修复 IVT 页顶栏重构造成的标记测试回归：`test_overview_markup_matches_information_hierarchy` 改为断言新页面标题，并补充风扇页标记断言。
- 主监视快照不再携带风扇数据（含 40 条应答历史），风扇页继续通过独立的 `fan_snapshot()`/`fan_state()` 获取。
- 清理界面残留：删除 `renderFan` 中无对应元素的 `#fanCurrentBus` 输出和只写不读的 `inputsInitialized.fan`；“运行模式设定”头部的 `0x01` 固定为模式命令操作码标签（加悬浮说明），不再随当前模式变化。

### 2026-08-04

#### P2 台架、P3 IVT 配置与页面拆分

- 历史：主上位机连接窗口增加“CAN1 从控台架”。台架只发送 138 串/48 路从控帧，当时真实 IVT-S 接 F405 CANB；2026-08-30 已迁到 CAN1，台架模拟器仍不发送 `0x512`。
- 主上位机增加独立“PCAN 从控台架”页面，场景按钮和命令输入复用 `pcan_bms_bench.py` 的 `BenchModel` / `CommandProcessor`。
- 历史：新增 ivt.py，统一 IVT 请求、响应解析、当时的 BMS CANB 目标配置和读回比较；2026-08-30 目标已改为 CAN1。IVT 应答由 CanService 的接收线程分发，配置操作不再另开 recv。
- 历史：当时 CANB 允许的写操作限定为真实 PCAN 上的 IVT 命令；2026-08-30 IVT 配置已迁到真实 CAN1，整车 CANB 不再接收或配置自有 IVT。
- 主上位机增加独立“IVT 配置”页面。配置会写入 8 个通道、10 个 CAN ID、正负过流阈值，保存、重启并读回；页面名称、命令边界和 `pcan_ivt_tool.py` 对齐。
- 增加 IVT 协议、出厂默认/目标/差异分类、ID 变更和 CAN1/模拟连接拒绝测试。

#### 审查项收敛和 Legacy 固件

- 修复 Q3 归因只按实际 Q3 保持/输出状态判断，避免 Debug-Bringup 关闭一级保护或告警刚出现的窗口把外部 HV_ACC 释放误记为 BMS 动作。
- 补充 CAN2 位率切换失败后的回滚结果和“位率未知”状态；失败恢复时清除旧 IVT 有效性，避免继续使用切换前数据。
- RTC 请求改为排队，连续请求不再互相覆盖；RTC 队列满返回状态 7，上位机按应答 Byte1 序列回显匹配。
- 故障帧与告警等级帧要求同时新鲜才组合显示；2 秒低频回报使用 2.5 秒窗口；断开/换设备清除 RTC 和充电计时旧状态；CANB 无单体原始数据时显示“无数据”而不是“模组离线”。
- `.bmslog` 加载阶段逐行校验所有帧；模拟器按 CAN1/CANB 边界、参数范围、保留字节和命令拒绝规则工作。
- 增加 `Release-Legacy` 固件身份解析。

- 本轮验证：50 项 Python 主机测试、PCAN 工具 Python 语法、JavaScript 语法和 `git diff --check` 通过；实体双 PCAN、真实 IVT、CANB 满载和 Windows 发布环境仍待验证。

#### 数据过期显示和台架边界

- 配置、单体累加、单体极值、继电器请求回读和运行诊断分别保留回报年龄；过期时参数控件清空或禁用，故障页和电芯页不再用旧阈值继续判断，过期的充电机类型不会覆盖“等待数据”。
- 历史：`pcan_bms_bench.py` 默认只在 CAN1 发送从控帧，当时真实 IVT 接 CANB；2026-08-30 IVT 已迁到 CAN1。启用 `--canb-sop-ack`（兼容别名 `--sop-ack`）时，现在只处理 ECU SOP `0x4A4`，不接收或发送 IVT。
- 增加单体累加/极值、阈值/开关/运行诊断独立过期测试、失联后趋势不再追加旧电气值，以及 CANB 上误发工具命令时优先提示总线限制的测试。

#### 文档归并

- 将原主控接口、安全边界和项目级验证内容并入 `DOC/CAN上位机与工具使用.md`，`DOC/` 只保留一份上位机使用文档；本目录 `todo.md` 继续作为上位机唯一的进度与变更记录。

### 2026-08-03

#### 协议版本4同步

- 所有工具请求使用 `0x18A050F5`、固定 DLC=8；CANB BMS 状态/故障/告警使用 `0x4B0/0x4B1/0x4B2`，Chroma 反馈使用 `0x201/0x202/0x204/0x205`；`0x186050F4` 按有符号电流和有效位解析；模拟器同步。无效或不完整的单体/温度极值不再显示为占位值或旧值。

#### 总状态有效性同步

- 总览只把 CAN1 `0x186050F4` 或 CANB `0x4B0` 作为包电压、电流、SOC 和单体/温度完整性的有效来源；`0x187650F4`/`0x4B1` 故障帧不再延长总状态的新鲜时间。
- 总状态帧超时或对应有效位为 0 时，包电压、电流、SOC、单体极值和温度极值显示“等待数据”，不继续显示上一条旧值；补充协议解码测试覆盖故障帧与总状态帧的独立超时。
- 写命令后端也要求最近 1.5 s 内收到总状态帧，不能仅凭故障帧中的旧状态继续发送配置或复位请求。

#### 提交前复核修正

- 修正构建日期解析的非闰年 2 月 29 日边界；运行诊断帧过期时不再回显旧充电机反馈；当时的故障帧版本3把 bit22 显示为“事件触发”，当前版本4已改为保留并由高压诊断位显示外部安回事件；补充日期边界测试。

#### 参数页和 Windows 发布入口收紧

- 参数页压缩标题、输入框、开关行和维护命令行的高度；“系统参数与维护命令”和“需要谨慎的操作”拆成左右两个同级面板，Flash 保存状态移到顶部命令条件栏。
- 充电请求回读拆成“请求值”和“充电机反馈”两个字段，分别表示主控保存的请求值与 Legacy/Chroma 充电机返回的实际值。
- 源码运行仍保留内置模拟数据供开发；PyInstaller 发布版固定使用真实 PCAN，隐藏工作模式选择并排除模拟器模块。发布版使用说明补充 Python、PCAN-Basic/驱动和 WebView2 的安装边界。

#### 状态机、IMD 和保存状态再次精简

- 状态机改为“状态机”标题在上、DOC/代码短状态名在下的纵向布局，使用 `SELF_TEST`、`STANDBY`、`PRECHARGE`、`HV_ON`、`FAULT` 并按状态着色。
- IMD 总览只保留 `OK_HS` 正常/故障、实际 PWM 频率、占空比和绝缘阻值；DCP、SST 等模式通过简短悬浮提示说明，不再铺开“有信号/有效/通过”重复字段。
- 保护开关显示 DOC 中的 CLI 短名，完整变量名放在悬浮提示中。悬浮提示只说明字段含义或缩写，不展示实现层面的无效值和兜底处理。
- 配置、告警开关、充电请求、电流方向和充电机类型写入后，界面继续读取主控保存状态；仍在保存时显示等待，保存等待位清除后显示“已保存到 Flash”。模拟器同步覆盖保存等待和完成状态。

#### 状态机视觉样式统一

- 移除状态机信息条独有的彩色左侧竖条和动态颜色变量，恢复与其他卡片一致的普通边框、圆角和间距。

#### 总览信息层级二次修正

- 高压输出直接用三个等宽状态格显示主正、主负和预充继电器，移除“继电器控制命令”等不能增加信息的小标题；请求、安全回路和最近一次预充保留为紧凑字段表。
- 温度与冷却、绝缘监控（IMD）拆成两个独立卡片；IMD 标题、状态值和 PB8/PWM/阻值/绝缘判定的字重与字号统一提高，避免诊断结果被小字淹没。
- 充电计时卡片只显示已计时和预计剩余时间；平均充电电流仍只作为后台估算输入，不再占用总览空间。
- 修正无数据时把 `0x00000000`、无活动告警或未充电显示成确定状态的问题；故障、告警等级、继电器命令、温度/风扇和 IMD 报文超过 1.5 s 后统一显示等待数据，顶部告警颜色也恢复为绿色/黄色/红色状态提示。
- 充电计时改为每次轮询都推进，不因切换到其他页面而漏计；内置模拟数据的高压阶段增加 `charge_mode=1` 和正向充电电流，能够复现计时与预计剩余显示。
- 将“安全状态优先、主题单一、状态值醒目、按内容分配空间、只保留现场需要的信息”写入项目 `AGENTS.md` 与上位机 `README.md`，作为后续界面修改的检查依据。

#### 状态信息和页面布局整理

- 删除左下角“FORMULA STUDENT”文字，保留 BITFSAE 标志；左下角将上位机版本和日期合并为一行，并改为显示硬件 PCB V3.0（2026-05-30）。
- 移除每个页面共用的顶部标题和解释栏，把连接设备/模式按钮移到底部状态栏。
- 固件身份显示变体、Git 标识和北京时间构建日期，不再显示构建工作区的“未提交改动”；底部不再显示状态帧年龄。状态帧新鲜度判断仍在后台保留。

#### 品牌、趋势图和界面一致性

- 左下角文字标志替换为 BITFSAE 官方标志（亮色字标），侧栏顶部使用银鲨图标。
- 趋势图重做为包电压、总电流和 PRE 电压三线双轴曲线：左右轴带刻度标签、时间轴、零线、渐变填充和末端数据点，图例实时显示当前值；模拟数据也输出 PRE 电压。
- 单体页异常框改为外扩描边，相邻异常单体共用连续红色轮廓，不再出现红框之间的深色间隙。
- 统一按钮尺寸：主操作按钮不再独占整行，改为与页面右对齐的紧凑按钮，与“立即校时”“写入”等操作按钮一致。
- 参数页告警开关行增加状态圆点和“启用/关闭”文字，启用行使用绿色状态（与故障页保护开关一致），修改后显示“待写入”。
- 底部状态栏统一为单行：连接项显示工作模式、总线用途和位率；RX/TX、固件身份和记录状态使用统一层级，回放使用黄色状态点，Legacy CANB 不重复显示 250k。
- 移除工程参数文件（`.bmsproj`）功能：按钮、对话框、导出/导入接口和对应测试一并删除，简化顶部栏。
- 固件新增独立构建日期帧`0x186C51F4`（经典CAN DLC 8：年-2000、月、日，北京时间），与身份帧同时发送；状态栏固件信息显示“变体 · Git · 构建日期”，旧固件不发该帧时不显示日期。
- 首次验证 macOS 源码运行：Homebrew Python 3.14 虚拟环境安装 pywebview 6.2.1 与 python-can 4.6.1，`python -m TOOLS.bms_host` 正常打开原生窗口；macOS 无 PEAK 驱动，只能使用内置模拟数据。

#### 故障页和界面缩放复核

- 重做“故障与记录”页：删除告警卡片内重复出现的序号和bit文字，改为直接显示主控阈值、固件固定条件及确认/清除时间；一级、二级、故障码置位、正常和保留项使用统一且可辨认的状态样式。
- 保护开关改为四列对齐的“启用/关闭”状态表，增加启用数量和回报版本汇总；启用项使用明确的绿色状态，关闭项降低层级。
- 默认界面缩放调整为110%，`Ctrl+0`恢复110%；修复放大后工作区仍按未缩放的100vh计算、导致底部状态栏和下方内容超出可视区的问题。
- 告警等级帧和保护开关帧未收到时明确显示“等待等级/等待回报”，不再把尚未收到的数据误显示成“正常”或“关闭”。
- 重整总览信息分组：高压链路、温度/风扇和 IMD 分开显示，详细字段见 `README.md` 的“运行总览”。
- 再次压缩总览首屏：缩短状态条、KPI、表头和数据行，降低次级说明字号，删除高压卡片常驻解释文字，并缩短趋势图高度，让更多重要状态在首屏可见。
- 完善 IMD `0x186850F4` 解析：读取 PB8、PWM、阻值有效、绝缘通过等 Byte1 位；PWM 无效时隐藏频率/占空比，阻值无效时显示 `—`，`0xFFFF` 饱和阻值显示 `≥65535 kΩ`，并对 IMD 帧增加新鲜度判断。

### 2026-08-02

#### 界面结构和交互复核

- 删除每个模组内部重复占行的“电压”“温度”标题；放大全包统计值，并把四项保护阈值改为独立、清晰的状态条。
- 增加应用内页面缩放处理，修复部分Windows WebView2窗口设置`zoomable=True`后`Ctrl++`仍无响应的问题；现在明确捕获主键盘和数字键盘的加减号、`Ctrl+0`及`Ctrl+鼠标滚轮`，缩放范围80%..130%并记住比例。
- 重新整理电芯与温度页的信息层级：正常模组不再反复显示“数据完整”，只有通信中断或数据缺失时才突出提示；去掉重复的全局编号范围，单路编号改为模组内 `C1..C23` 和 `T1..T8`。
- 电芯页顶部只保留最高、最低、压差和温差等直接用于判断的值，完整性仅在缺失时提示；模组标题改为电压范围、压差和温度范围，并放大单路数值与标签；温差统一保留一位小数，避免浮点尾数直接显示。
- 将工具运行产生的 `.mcp-npm-cache` 加入忽略列表，并撤销与本次上位机运行和界面修正无关的打包脚本改动，减少工作区噪声。
- 恢复包电压/总电流双轴趋势曲线；新增从检测到充电模式开始的本次计时，并按16.2Ah、SOC和本次平均充电电流显示预计剩余时间与预计充满时刻。
- 统一放大页面标题、导航、区块标题、表格、状态值、输入框和按钮的字号与行高，消除同级组件大小不一致的问题；1120×720仍无水平溢出。
- 重新划分文档职责：主控接口、安全边界、界面、操作、连接和排错说明统一维护在`DOC/CAN上位机与工具使用.md`，本目录`README.md`只保留安装、运行和发布，`todo.md`保留进度、风险、验证和历史变更。
- 将信息架构分成四个只读监视页和一个“参数与命令”写入页；读取Flash日志保留在故障页，所有会改变主控状态或参数的操作集中到写入页。
- 总览改为现场状态表：状态机、故障码与解析、包电压/电流、PRE电压、压差、SOC、继电器、HV_ACC、充电信号、安全回路、预充耗时、六个模组平均值、风扇和IMD；删除从控完整性卡片和SOP面板，保留电压/电流趋势曲线。
- 138串电压和48路温度改为每个BMU内同页并列显示，顶部同时显示主控当前阈值。
- 删除侧栏选中竖条、选中背景框和同类装饰性竖线；左下角增加BITFSAE、软件版本、版本日期和目标主控。
- 修复PyWebView启动后递归扫描原生WinForms窗口的问题：后台服务和窗口引用改为私有成员，不再通过JavaScript API暴露`AccessibilityObject`，避免`TYPE_E_CANTLOADLIBRARY`和递归深度溢出导致界面卡死。
- 前端状态读取改为单次完成后再安排下一次，禁止250ms轮询重叠；总览保持250ms，其余详细页使用500ms，窗口隐藏时使用1000ms。每轮只更新当前页面需要的内容。
- 将界面调整为紧凑的现场仪表结构，减少重复容器、过大圆角、分散间距和装饰性动画。
- 总览按主控状态、四个关键量、高压链路、从控、活动告警和诊断分项组织信息。
- 单体页在宽窗口按8列显示电压，并在“只看异常 / 缺失”时隐藏全部正常的从控分组；全部正常时只显示一次结果。
- 告警矩阵在宽窗口使用4列，较窄窗口使用3列；每项在固定位置显示名称、判定条件和状态。
- CAN监视器改为报文表在窗口剩余区域内独立滚动，搜索、筛选、暂停、记录和回放操作保持可见。
- 修复长页面切换到短页面时沿用旧滚动位置，导致新页面看起来空白的问题。
- 修复充电机类型选择在发送前被250ms周期回报立即覆盖的问题。

### 2026-08-02（初始实现和协议扩展）

- 新增PyWebView Windows上位机、独立CAN接收线程和内置模拟数据。
- 完成CAN1 138串/48路温度、主控状态、高压、IMD、SOP、32项告警、故障码变化记录、阈值、19个开关、充电请求和维护命令。
- 支持CANB 500 kbit/s、Legacy CANB 250 kbit/s、`.bmslog`/CSV记录与历史回放、`.bmsproj`工程导入导出。
- 接入版本3工具命令、统一应答、固件身份、充电机反馈、IVT/SOC诊断和Flash重要故障日志读取。
