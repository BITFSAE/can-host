# 进度与待办

本文件只记录当前状态、未完成工作、风险和最近验证。已完成版本见 [`CHANGELOG.md`](CHANGELOG.md)，操作说明见 [`DOC/CAN上位机与工具使用.md`](DOC/CAN上位机与工具使用.md)，开发与发布见 [`DOC/开发与发布.md`](DOC/开发与发布.md)。固件侧工作留在对应固件仓库；跨层问题在两边各留一条并互相指向。

## 当前状态

| 范围 | 状态 |
|---|---|
| BMS | CAN1 详细监视与统一 CANB 镜像、138 串电压、48 路温度、状态、故障、记录回放和受控工具命令已接入 |
| 整车 CANB | 单一物理 CANB 连接同时供给 BMS 镜像、SOP、PDM、ECU、胎温、赛会能量计和两套风扇 |
| 工程工具 | CAN 监视与发送、F405 从控台架、IVT 配置、风扇标定和本地遥测模拟已接入 |
| 遥测 | MQTT 只读订阅、BMS 故障变化记录及 MQTT/串口/PCAN 本地模拟已接入 |
| 发布 | v0.9.16 已发布到 GitHub/CNB，双平台 7 个附件齐全；目标机实体硬件验收仍待完成 |

当前协议边界：F405 工具协议版本 5，告警开关帧版本 7；上位机不定义新 CAN 契约。精确帧定义以固件和 vehicle-interfaces 为准。

## 待完成

### 发布与平台

- [x] 已用真实 `vX.Y.Z` 标签跑通完整发布并核对说明来源一致：v0.9.6–v0.9.8 的 GitHub Release 正文、CNB 频道 `latest.json` 和随包说明由 `CHANGELOG.md` 同一小节生成（v0.9.7 为 11 条、v0.9.8 为 2 条），更新弹窗读的就是同一个 `release_notes.release_notes_from_body`。
- [ ] 在干净 Windows 10/11 验证发布目录离线完整性、安装、快捷方式、WebView2、PCAN、软件更新、失败回滚和卸载。
- [ ] 在干净 Apple Silicon Mac 验证 DMG 安装、Gatekeeper、两步软件更新、健康确认与失败回滚、只读位置的 DMG 回退以及文件对话框。
- [ ] 发布包含 certifi 的包后，在 macOS 实机验证更新检查不再报证书错误；在此之前的过渡期，手动下载新 DMG 覆盖安装。
- [x] 已发布 v0.9.5–v0.9.12 双平台标签；v0.9.12 起新增 macOS 自动更新 ZIP/校验，共 7 个附件，发布 Action 与 CNB 更新频道同步完整通过。
- [ ] 验证私有 GitHub 仓库的令牌保存、清除、401/403/404 和附件下载。

### 实体 CAN 与车辆

- [ ] 把 PCAN 实体接到对侧总线复核疑似接反提醒：弹窗时机、页面警示条、参数与命令页徽标，以及改接正确总线后的自动恢复。
- [ ] 用实体 PCAN 验证自动枚举、热插拔刷新、CAN1/CANB 物理通道分配与交换、连接、断开、重连、同句柄冲突、双通道并发和错误提示；Windows 上分别覆盖单通道 PCAN-USB 与双通道 PCAN-USB Pro FD，macOS 手动列表覆盖 BUS1/BUS2 实际端口对应关系。
- [ ] 对照 PCAN-View/F405 实帧核对 138 串、48 路温度、状态、告警、继电器、安全回路和 IMD。
- [ ] 验证 F405 阈值、开关、充电参数、RTC、故障复位和 Flash 日志的应答、周期回报与断电保存。
- [ ] 实车 CANB 逐卡核对 SOP、BMS 镜像、PDM、ECU、胎温和赛会能量计；确认胎温轮位及 `0x526/0x528` 是否实际发送。
- [ ] 双 PCAN 并发时验证快捷栏高压/SOC 的主连接优先与 `0x4B0` 回退。
- [ ] 长时间记录 `.bmslog`/CSV，检查高负载下的完整性、文件增长和回放性能。

### 专项工具

- [ ] 用实体 USB-RS485 验证本地遥测模拟器串口输出的连续帧边界、目标波特率和可选包尾。
- [ ] 用独立实体 PCAN 测试总线验证本地遥测模拟器的 CAN1 输出、停止收尾和通道占用提示。
- [ ] 在 Windows 安装版上打开“工程工具 → 遥测数据模拟”，确认三种输出（MQTT / 串口 / PCAN）都能选到设备并启动；下次 Windows 构建会先跑冻结包自检，本机只验证过 macOS 分支与规格分析。
- [ ] 用真实 IVT 完成读取、配置、重启、周期核对和 CAN1 接入。
- [ ] 用真实 FanController 验证两档扫频、租约、参数拒绝、保存和断电加载。
- [ ] 用实体 FanController 制造 1.5..10 秒 PDM/风扇状态断流，确认 `0x5A9 flags.bit0` 置位、输出回安全自动、上位机显示暂停且恢复后仅重做当前点；再断流超过 60 秒确认租约最终中止，并核对自动诊断 JSON 的原因码、步骤、目标和帧年龄。
- [ ] 用实体 FanController 复测低占空比起转死区：`0x5A3` TACH 位不再让上位机提前停止，扫描应继续越过 5%/8% 等 0 RPM 点；风扇正常起转后制造真实停转，确认固件 `0x5A9` 中止仍能立即收尾。
- [ ] 将 `../FanController` 的连续零转速 500ms 确认版刷入实体板，重扫回路 1 电池档；单帧 TACH1 归零时 `0x5A9` 应保持活动并完成上升/下降点，受控持续断开各路 TACH 时仍应上报原因码 5。复测背景负载稳定性后再确定 PWM 上限；固件待办见 `../FanController/CHANGELOG.md`。
- [ ] 用真实 F405 验证电池箱风扇查询窗口、高压标定、安全中止、保存等待和断电加载。
- [ ] 实车验证自动标定的命令 ACK 后 `0x5A9/0x5AD` 目标匹配、两套标定互斥和断开连接后的设备租约归零；同时验证电池箱提交按钮按最后已知完成状态使能在实车上不会被过期状态误导。
- [ ] 用台架实测值替换 FanController 功率预测占位表；当前标定只保存安全 PWM 上限。
- [ ] Chroma 35 W 上限完成实物复测；当前由高压/DCDC 曲线推导，没有独立充电侧自动扫频。
- [ ] 在非默认物理总线上验证通用发送；确认项目命令 ID 不能绕过专用页面。
- [ ] 用现网只读账号验证 MQTT 重连、故障变化、3 秒断流和损坏 Payload 计数。

### 界面

- [ ] 在 Windows WebView2 和 macOS WKWebView 实机各冷启动一次浅色、深色主题，确认原生窗口首帧不闪烁，并在 Windows 100%、125%、150% 显示缩放下复核侧栏切换按钮与原生下拉框。
- [ ] 连接实体双通道 PCAN（CAN1 与统一 CANB 同时）复核 BMS 数据源优先级、整车总览与风扇页在真实数据下的数值宽度、过期态配色和长文本换行；浏览器预览已覆盖双路在线、仅 CANB 回退、模拟数据下的几何与过期态。
- [ ] 在 Windows PyWebView 的 100%、125%、150% 显示缩放下检查最小窗口、对话框、表格和输入焦点。
- [ ] 用实体 F405、PDM 和 FanController 制造间歇丢帧，验证电芯、整车、PDM、两套风扇及快捷栏的最后有效值持续显示、年龄递增且新鲜度、离线和控制门控不被误判；F405 另覆盖从控 70 ms/帧调试模式。
- [ ] 录制一次完整充电台架流程，按操作者顺序复核信息和按钮位置。

## 当前风险

| 风险 | 关闭条件 |
|---|---|
| 模拟数据不能证明驱动、总线时序和实体帧正确 | 完成 Windows/macOS 实体 PCAN 与 F405 台架验收 |
| macOS 依赖第三方 `libPCBUSB`，当前只验证本机构建和加载 | 在干净 M 系列 Mac 完成双通道收发和命令应答 |
| 通用发送无法识别所有外部节点的危险命令 | 只在明确物理总线和帧定义时使用；持续维护受保护 ID 清单 |
| 整车节点、胎温轮位和赛会能量计部分行为仅有模拟验证 | 完成实车 CANB 逐卡记录并同步 vehicle-interfaces |
| 高总线负载下的长时间记录性能未知 | 完成长时间记录、停止、重开和回放测试 |
| Windows 自动更新和回滚尚未在目标机真实验收 | 在干净 Windows 10/11 完成旧版升级、失败回滚、卸载入口和 PCAN 验收 |
| MQTT 1883 为明文且共用只读账号 | Broker 部署 TLS 并为上位机创建独立只读账号 |

## 最近验证

| 日期 | 结果 | 未覆盖 |
|---|---|---|
| 2026-09-25 | 对照 `fan_calibration_20260925_175418.json` 和 `fan_calibration_20260925_181244.json` 所指向的两段 CAN 原始记录：首轮在 30% 测点以原因码 5 中止，升速至实际 28% 时曾有一帧 TACH1 归零，二者相隔约 5 秒；该轮 PDM 还发生独立的背景负载跳变。复测在 100% 测点出现 TACH1 单帧归零，约 20ms 后立即中止，而 PDM 总线电流稳定在 3.98–3.99A，排除了背景电流跳变为中止的必要条件。`../FanController` 已将启动和运行停转判定改为逐风扇连续零转速 500ms，保留 3 秒起转宽限与其他安全门槛；固件 Debug/Release 构建、原生时间边界测试及上位机风扇 48 项测试通过。 | 尚未把修改后固件刷入实体板；必须实测单帧误零不再中止、持续停转仍按原因码 5 中止，并复测完整电池档升降曲线。 |
| 2026-09-21 | 调试模拟入口与 Windows 更新准备阶段收敛：本地源码运行保留“调试模拟”，Windows/macOS 冻结包默认隐藏入口、后端拒绝模拟模式，macOS 规格排除整车模拟器；更新下载不再同步清理旧解压目录，更新包先于独立校验文件请求，连接/下载/校验信息/校验/解压分别显示且不可量化阶段不再显示假 0%。全量 334 项测试通过（1 项平台跳过），全部 JavaScript 语法、Python 编译、差异检查及完整依赖环境源码打包自检通过。 | 尚未在 Windows WebView2 发布包复测首次下载等待时间、Defender 开启时的阶段切换和真实更新安装；两个冻结发布包的“调试模拟”隐藏与后端拒绝待下一轮双平台产物自检。 |
| 2026-09-21 | 发布 v0.9.16：单通道轮换与接反交换后端原子化、CAN 连接设置弹窗与底栏重排三项变更打包发布。发布前本机全量 332 项测试通过（1 项平台跳过），Python/JavaScript 语法与差异检查通过；GitHub 主分支 CI、Windows/macOS 构建、GitHub Release 和 CNB 同步全部成功，GitHub 与 CNB 更新频道均为 7 个附件，macOS 更新 ZIP 的 CNB 匿名下载 SHA-256 与发布校验文件一致。 | 未在实体 PCAN 上验证轮换与交换的驱动释放/重连时序、失败回滚和自动留档收尾；交换弹窗的浏览器交互与窄窗口排版未实测。 |
| 2026-09-21 | 单通道轮换免手动断开与接反一键交换：同通道冲突从后端拒绝改为同一把连接锁内先断后连，成功按普通状态提示，目标连接失败自动恢复原连接；接反交换改为后端原子流程，执行前再次核对双向证据并拒绝中止两类风扇标定，任一路重连失败时回滚 CAN1/CANB 原连接，全部成功后才保存新分配。全量 332 项测试通过（1 项平台跳过），新增双向接管、单路失败恢复、交换成功、标定占用拒绝与部分失败双路回滚测试；Python/JavaScript 语法和差异检查通过。 | 未在实体 PCAN 上验证切换、交换与失败回滚的驱动释放/重连时序和自动留档收尾；交换按钮的浏览器交互与弹窗排版未截图验证。 |
| 2026-09-21 | 重整 CAN 连接设置弹窗与底栏：设置按钮移入连接坞（CAN1/CANB/调试模拟旁）并统一为带边框方块图标；弹窗改为卡片 + 中间交换图标按钮的三列布局，副标题独占一行使两个下拉框对齐，字段标签精简为“PCAN 通道”，正常态不再复读通道分配，仅冲突/未选时提示，底部说明文字删除。全量 325 项测试通过（1 项平台跳过），新增交换按钮位置与标签去重锚定；`node --check` 全部脚本通过；用 ui_preview + Playwright 截图核对冲突警示、交换自动错开、正常态与底栏布局。 | 未在 Windows WebView2 与窄窗口实机核对弹窗排版；窄屏（≤680px）交换按钮竖排样式仅按 CSS 规则实现，未截图验证。 |

| 2026-09-21 | 发布 v0.9.15：删除旧充电机与 250 kbit/s 兼容路径、CAN1/CANB 双物理总线连接模型、风扇标定安全暂停三项变更打包发布。发布前本机全量 325 项测试通过（1 项平台跳过），本机 macOS 完整构建、冻结包自检、PCAN 驱动加载、最低系统版本、签名、DMG 与更新 ZIP 解包校验全部通过；GitHub 主分支 CI、Windows/macOS 构建、GitHub Release 和 CNB 同步全部成功，GitHub 7 个附件齐全，Release 正文由 `CHANGELOG.md` v0.9.15 小节生成。 | 尚未在 Windows WebView2/macOS WKWebView 与实体 PCAN、IVT 上验证固定 500 kbit/s 连接、双通道并发、配置与异常拦截。 |
| 2026-09-21 | 主分支删除旧充电机与 250 kbit/s CANB 兼容路径：去除旧连接档案、反馈帧解码、类型/固件变体字段、切换命令、模拟帧、IVT 位率切换界面及相关 CLI 入口，CANB 与 IVT 配置连接固定为 500 kbit/s；旧实现已保留在 `codex/legacy-charger-250k` 分支。全量 325 项测试通过（1 项平台跳过），Python/JavaScript 语法、差异检查和完整依赖环境源码打包自检通过。 | 尚未在 Windows WebView2/macOS WKWebView 与实体 PCAN、IVT 上验证固定 500 kbit/s 连接、配置与异常拦截。 |
| 2026-09-20 | 完成 CAN1/CANB 物理连接模型重构：统一 CANB 单句柄同时驱动 BMS 镜像和整车协议，CAN1 缺席时 BMS 总览/故障自动使用 CANB 投影，电芯与 F405 命令仍锁定 CAN1，风扇写入仍锁定真实 CANB 500 kbit/s；底栏收敛为 CAN1/CANB，连接设置改为两路物理通道分配，自动枚举双通道时默认错开，手动列表保留旧选择并提供自动分配/交换与同句柄警告，后端双向阻止并发占用。新增共享接收流与通道冲突回归测试，全量 325 项测试通过（1 项平台跳过），Python/JavaScript 语法、差异检查和完整依赖环境源码打包自检通过。浏览器预览实测 1460×920 深浅主题、600×800 与 375×812 窄窗口、旧同通道配置迁移、自动分配/交换/冲突提示，以及仅 CANB 时 BMS 镜像生效、CAN1 电芯提示保留、监视器自动选 CANB；控制台无错误。 | 尚未在 Windows WebView2/macOS WKWebView 与实体双通道 PCAN 上验证端口对应、热插拔、真实并发收发、记录和命令应答。 |
| 2026-09-20 | 完成 FanController 自动标定可恢复暂停改造：PDM 与 `0x5A2/0x5A3/0x5A8/0x5A9` 超时统一为 1500 ms，短时断流、温度输入无效和命令确认超时改为安全暂停，固件保持 ACTIVE 并退出越限扫描，数据恢复后仍保持安全输出，直到上位机重发并完整重做当前测点；60 秒租约由独立 5 秒心跳续期，续租与恢复命令串行，避免零目标覆盖恢复目标。基线/测点最多完整重采 3 次，终止诊断原子写入配置目录。上位机全量 322 项测试通过（1 项平台跳过，另有 24 个子测试），Python/JavaScript 语法和源码打包自检通过；FanController Debug/Release 构建通过（RAM 2776/2768 B，FLASH 36180/22052 B）；节点与中央 DBC 经 cantools strict、canmatrix、cancompare 及接口总校验通过，语义差异仅为 `0x5A9` 注释。 | 尚未刷入实体 FanController 制造 1.5..10 秒及超过 60 秒断流，核对安全输出、暂停旗标、当前点重做、硬安全条件和诊断 JSON。 |
| 2026-09-20 | 修复两套风扇标定中止后的记录导出：确认 CAN 监视记录的系统保存对话框路径正常，风扇导出改用同一机制，不再让 WKWebView/WebView2 处理 Blob 下载；整车风扇支持 CSV/JSON，电池箱风扇支持 CSV，保存成功显示实际路径，取消不报错。新增中止会话三种文件写入、取消保存和前端连线测试；全量 317 项测试通过（1 项平台跳过），JavaScript 语法、Python 编译、差异检查和源码打包自检通过；v0.9.14 本机 macOS 完整构建、冻结包自检、PCAN 驱动加载、最低系统版本、签名、DMG 与更新 ZIP 解包校验全部通过。 | 尚未在复现问题的 Windows WebView2/macOS WKWebView 上用实体标定会话分别验证手动中止、保护中止后的系统保存对话框和文件内容。 |
| 2026-09-19 | CAN1/CANB 疑似接反提醒与数据页连接提示条：后端按“对侧独有帧”判定（CAN1 档案收到整车 CANB 节点帧、CANB 档案收到 F405/从控 CAN1 专属扩展帧，相邻证据不超过 2 秒、至少 2 帧且最后证据 3 秒内），在主/整车/快捷快照的 connection 里标注 `bus_mismatch`，只提醒不改变任何后端命令门控，模拟/回放/台架不参与；前端每次连接只弹窗一次并点亮相关页面警示条，参数与命令页徽标接反时显示“CAN1 疑似接反”且保留状态点。总览、电芯、故障、整车总览四页新增连接提示条，BMS CANB 不再错误隐藏电芯页的 CAN1 提示。`Tests/test_bus_mismatch.py` 共 19 项，全量 315 项测试通过（1 项平台跳过），改动 JS `node --check`、Python 编译与差异检查通过；前一轮本地 HTTP 服务 + 浏览器注入模拟快照已实测未连接、正常 CAN1、CAN1 接反和整车连接四个场景并截图复核。 | 本轮修正后的状态点、BMS CANB 电芯页提示和同连接弹窗去重尚未在 Windows WebView2/macOS WKWebView 实机视觉复核；未在实体 PCAN 上制造真实接反复核判定窗口与弹窗时机。 |
| 2026-09-19 | 根据 Windows v0.9.13 实机复现继续修正失效代理回退：确认 `urllib` 的 HTTPS `ProxyHandler` 会原地把 Request 改为代理主机，旧实现随后复用同一对象，所谓直连仍指向已关闭的本地代理。现于首次请求前复制原始 URL、方法和普通请求头，直连使用独立 Request 且剔除代理凭据；直连本身失败时保留底层原因。新增请求改写回归测试，全量 296 项测试通过（1 项平台跳过），Python 编译、差异检查和源码级打包自检通过；另用拒绝连接的 `127.0.0.1:1` 作为系统代理驱动真实网络路径，已自动直连 CNB 并读到 v0.9.13。 | 待在该 Windows 目标机关闭代理软件并保留系统代理后复测 CNB 检查与更新包下载。 |
| 2026-09-19 | 发布 v0.9.13：底栏三个真实 CAN 连接按钮不再强制跳转监视器，并纳入风扇标定数据质量与中止诊断修复。发布前本机 295 项测试通过（1 项平台跳过），JavaScript 语法、Python 编译、发布说明与差异检查通过；GitHub 主分支 CI、Windows/macOS 构建、GitHub Release 和 CNB 同步全部成功。GitHub 与 CNB 均有 7 个附件，CNB 更新频道已指向 v0.9.13，7 个附件匿名范围读取均成功。 | 待在 Windows WebView2、macOS WKWebView 和实体 PCAN 下复核三个底栏连接按钮保持当前页面；实体风扇标定仍需复现背景负载波动并核对导出诊断。 |
| 2026-09-19 | 底部 CAN1、BMS CANB、整车 CANB 三个连接按钮改为只控制连接状态，不再在成功后强制跳转 CAN 监视器；仍为监视器预选刚建立的数据流。新增前端连线回归测试，全量 295 项测试通过（1 项平台跳过），两份改动 JavaScript 的语法检查与差异检查通过。 | 尚未在 Windows WebView2、macOS WKWebView 和实体 PCAN 下逐一点击三种连接，复核当前页面、焦点与滚动位置均保持不变。 |
| 2026-09-19 | 修正两套风扇自动标定把数据质量误当安全故障的问题：PDM 电流/功率波动超门槛时保留基线/测点、继续扫频并排除出自动推荐；有效样本不足仍中止，电流、温度、供电、遥测新鲜度和固件安全状态等硬门槛不变。整车 FanController 标定从会话启动起即可导出，零完整测点时 CSV 仍含状态/终止原因，JSON 保留已采原始样本与质量告警；电池箱风扇 CSV 同步保留诊断结果。风扇专项 40 项、全量 293 项测试通过（1 项平台跳过，另有 24 个子测试），Python/JavaScript 语法检查通过。 | 待实体 CAN 上复现背景负载波动，确认扫描不中止、待复核点不进入推荐，并核对首个基线中止时 CSV/JSON 文件内容。 |
| 2026-09-19 | 发布 v0.9.12：对照 Tidoc 已验证结构把 macOS 更新改为“下载更新 → 重启更新”两步流程，发布链路新增签名 `.app` 的 `ditto` 更新 ZIP/校验；更新器按平台选择、校验并安全解压归档，shell 助手交换整个 `.app`、按 PID/版本健康信号确认并支持失败回滚，不可写位置才回退 DMG。macOS shell 助手临时应用端到端替换成功；本机完整构建通过冻结包/PCAN 驱动/最低系统版本/签名自检，真实更新器下载、校验、解压并复核本机构建的更新 ZIP 成功。全量 291 项测试通过（1 项平台跳过），Windows 主分支 CI、Windows/macOS 发布构建、GitHub Release 与 CNB 同步全部通过；GitHub/CNB 的 7 个附件均可匿名访问，正式更新 ZIP 为 13,768,128 字节，公开频道由真实更新器代码选中 v0.9.12 的 macOS ZIP 与校验文件。 | 待在 `/Applications` 中用两个真实冻结版本实测下载、退出、Dock/窗口重启、健康确认、故障回滚，以及从只读 DMG 运行时的手动回退。 |
| 2026-09-18 | 同类时序审查确认并修复两条路径：FanController 的 TACH 超时改为在关中断后读取当前 tick，避免捕获时间戳领先造成 RPM 瞬间清零和假停转；F405 电池箱风扇 STOP 改为幂等，重复命令保留 COMPLETED 提交资格或 ABORTED 原始原因；上位机中止流程兼容新旧固件的 ABORTED/INACTIVE 零目标终态。风扇专项 37 项、全量 278 项测试通过（1 项平台跳过），Python 编译和源码打包自检通过；FanController 与 F405 Debug/Release 构建、F405 全部主机逻辑测试通过。 | 待刷入两套新固件并分别制造 TACH 边界脉冲、F405 安全中止和 STOP 应答丢失场景；DCDC 档 300ms 新鲜度策略保持原样。 |
| 2026-09-18 | 发布 v0.9.11：Windows 与 Apple Silicon macOS 构建、GitHub Release 和 CNB 发布产物同步全部通过，5 个附件齐全；发布前全量 277 项测试通过（1 项平台跳过），Python 编译、差异检查、源码打包自检及 Windows 主分支 CI 通过。版本包含 FanController 安全中止收尾修复和 Windows 失效系统代理直连回退。 | 待刷入 FanController `3108550` 并用实体风扇复跑完整标定；Windows 目标机仍需验证残留代理场景与软件内升级。 |
| 2026-09-18 | 分析 `CAN_2_20260918_180649.csv` 的 145516 行：5% 目标从 `18:05:47.933` 持续到 `18:05:53.592`，期间故障位始终为 0，证明低占空比保持修复生效；随后 `0x5A9` 以原因 2（PDM 超时）中止，但 CSV 中 `0x5A0/0x5A1` 各 1159 帧、最大间隔仅 128/129 ms。定位为 FanController 在控制循环先取 tick、后复制 CAN 中断快照导致时间戳偶发领先，无符号年龄回绕误报超时；固件改为复制快照后重新取 tick。上位机同时接受新鲜“已中止、目标归零”加 STOP ACK，避免等待协议上不会出现的“已完成”帧。风扇专项 36 项、全量 277 项测试通过（1 项平台跳过），源码打包自检、Python 编译和差异检查通过；固件 Debug/Release 构建通过。 | 待刷入新固件并用实体 FanController 复跑完整上升/下降扫频，确认不再出现原因 2 假中止；真实 PDM 断流仍须验证会在 1 秒后中止。 |
| 2026-09-18 | 定位 Windows 更新检查两路同时报 `WinError 10061` 为系统代理指向未运行的本地端口；更新器改为每次请求重新读取代理，拒绝连接时自动直连重试，并补充代理成功回退、直连 HTTP 错误保留、双路失败提示及非 Windows 不绕过代理的单测。全量 276 项测试通过（1 项平台跳过），Python 编译、差异检查和完整依赖环境下的源码级打包自检通过。 | 尚未在复现问题的 Windows 目标机上分别验证“关闭代理软件但残留系统代理”和“代理设置运行中发生变化”两种场景。 |
| 2026-09-18 | 发布 v0.9.10：Windows 与 Apple Silicon macOS 构建均通过，GitHub Release 与 CNB 同步成功；附件为 Windows ZIP/校验、Setup、macOS DMG/校验共 5 个。发布前全量 272 项测试通过（1 项平台跳过），Python 编译、差异检查和完整依赖环境下的源码级打包自检通过。 | 尚未在实体 FanController 上复跑低占空比完整扫频；目标机安装、升级及实体 PCAN 验收仍待完成。 |
| 2026-09-18 | 用 `CAN_2_20260918_173619.csv` 的 201301 行做流式统计并对照 FanController `fan_controller.c`：5% 步骤在 `17:36:03.350` 下发，`17:36:06.119` 仅产生低占空比启动未成功的 `0x5A3` TACH1/2 位，上位机于 45 ms 后误发停止，早于 6 s 测点完成。上位机现不再把活动标定中的 TACH 位直接当成堵转；配套固件在低于最低运行占空比时保持扫描目标，达到最低运行占空比后才重新开始 3 秒停转确认。上位机全量 272 项测试通过（1 项平台跳过），Python 编译和差异检查通过；固件 Debug/Release 构建通过。 | 尚未刷入实体 FanController 复跑完整上升/下降扫频；需确认 5%/8% 等 0 RPM 点被记录、起转点后 RPM 正常，并制造一次真实运行停转核对固件中止。 |
| 2026-09-18 | 修复检查更新偶发误判：实测确认旧实现会在 CNB 成功但落后时跳过 GitHub，也会把乱序列表第一项误当最新；现改为两个更新源并行核对、按最高有效语义版本选择，同版本保留 CNB 下载地址，无效标签跳过，检查线程注册/启动竞态与前端重叠轮询、桥异常一并收敛。新增 CNB 落后、双源同版、列表乱序、无效标签、重复检查和前端轮询回归测试；真实公网核对 v0.9.8 时选中 CNB，模拟本机 v0.9.7 时识别 v0.9.8；全量 272 项测试通过（1 项平台跳过），全部 JavaScript 语法、Python 编译、差异检查和源码级打包自检通过。 | 尚未在 Windows WebView2 和 macOS WKWebView 下人为断开单一更新源，核对超时期间界面反馈与关闭应用行为。 |
| 2026-09-18 | 新增持久化深浅主题：浅色按冷灰工程纸面重建表面、文字、边界、状态色、弹窗遮罩、滚动条和 Canvas 图表令牌，侧栏按钮即时切换并同步 WebView/应用设置，启动原生窗口底色跟随保存值；浅色最弱正文/状态文字组合对比度实算为 4.57:1。新增 5 项主题测试，全量 267 项测试通过（1 项平台跳过），全部 JavaScript 语法、Python 编译、差异检查及构建环境源码级打包自检通过；用真实 Chromium 在 1460×920 核对深浅总览、浅色电芯、过期风扇和连接弹窗，在 1120×720 核对折叠侧栏，最终控制台 0 错误/0 警告；减少动态效果模式下切换即时完成且不挂过渡类。浏览器复核同时发现并修正原生下拉箭头被组件背景简写重置后重复平铺的问题。 | 尚未在 Windows WebView2 与 macOS WKWebView 实机冷启动核对首帧、原生下拉控件和显示缩放；实体 CAN 故障/告警数据下的浅色状态仍待车辆或台架复核。 |
| 2026-09-15 | 发布 v0.9.8：Windows 与 macOS 构建、GitHub Release、CNB 同步全部成功，两端都是 5 个附件且字节数一致（ZIP 17511039、ZIP 校验 95、Setup 15735409、DMG 11739146、DMG 校验 106）；频道全量匿名校验 9 个版本 41 个附件全部可下载且 `latest.json` 顶部就是 v0.9.8；CNB 上两份 `.sha256` 的内容与 GitHub 记录的资产 digest 逐位一致（ZIP `4fd3264f…`、DMG `cd90876b…`）。用频道负载驱动真实更新器代码：解析出 2 条说明、选中 `BITFSAE_CAN_Host_v0.9.8.zip` 与其 `.sha256`，`release_is_newer("v0.9.8","0.9.7")` 为真，GitHub Release 正文与 CNB 频道正文的条目逐条相同。定版前复核 PCAN 枚举改动并核对所有实体连接入口：后端 `CanService.connect` 与 IVT 重连都不再回退默认句柄，前端连接弹窗、整车连接、台架、IVT 和遥测模拟器共用同一份动态列表，模拟器分区的禁用状态由 `setSimulatorSectionEnabled` 显式重设、不会被刷新覆盖；262 项测试通过（1 项平台跳过），三份改动 JavaScript 的 `node --check`、`compileall` 与 `git diff --check` 通过；源码级 `--packaging-smoke-test` 退出码 0；`build_macos.sh` 完成测试、PyInstaller、冻结包自检、`--pcan-driver-smoke-test`（本机装有 libPCBUSB）和最低系统版本核对（12.0、14 个 Mach-O）并产出 v0.9.8 DMG | 本机未连接 PCAN-USB；Windows 上单通道 PCAN-USB 与双通道 PCAN-USB Pro FD 的 `PCAN_ATTACHED_CHANNELS` 返回值、热插拔刷新、实体 CAN 收发、目标机升级与回滚、macOS DMG 覆盖安装仍待验收 |
| 2026-09-14 | PCAN 通道列表改为 PCAN-Basic 实时枚举：覆盖双通道设备的控制器序号、FD/IO 能力、可用/占用状态、空设备结果、旧驱动兼容回退与实体连接禁止隐式默认句柄；BMS、整车、台架、IVT 和遥测模拟器共用动态列表，连接设置支持热插拔刷新。新增 5 项枚举边界测试，全量 262 项通过（1 项平台跳过）；`.venv` 源码级 `--packaging-smoke-test`、Python 编译、三份改动 JavaScript 语法及 `git diff --check` 通过；本地浏览器实测连接弹窗的兼容回退状态、禁用空状态、刷新按钮和键盘可访问文本，布局无溢出。 | 本机未连接 PCAN-USB，尚未在 Windows 实测 `PCAN_ATTACHED_CHANNELS` 对单通道、PCAN-USB Pro FD 双通道、热插拔与外部占用的返回值；macOS 本机驱动拒绝该参数，已按设计进入手动兼容列表。 |
| 2026-09-14 | 发布 v0.9.7：Windows 与 macOS 构建、GitHub Release、CNB 同步全部成功，两端都是 5 个附件且字节数一致（ZIP 17505341、ZIP 校验 95、Setup 15729948、DMG 11736308、DMG 校验 106）；CNB 匿名校验 36 个附件全部可下载，频道 `latest.json` 顶部就是 v0.9.7；CNB 上两份 `.sha256` 的内容与 GitHub 记录的资产 digest 逐位一致（ZIP `85f2fc66…`、DMG `57dbbcf0…`）。用频道负载驱动真实更新器代码：解析出 11 条说明、选中 `BITFSAE_CAN_Host_v0.9.7.zip` 与其 `.sha256`，`release_is_newer("v0.9.7","0.9.6")` 为真，GitHub Release 正文与 CNB 频道正文的条目逐条相同。随本版本发布工作树里未提交的风扇标定指南重写与三处界面文案：指南中的 1.2 s 命令确认、基线每 4 点重采、FanController 6 s（3 s 稳定 + 3 s 采样）与 F405 5 s（2 s 稳定）、起始总线电流 `min(8.0 A, 保护值)`、未标定 15%/55% 封顶、F405 上报窗口 5 s·500 ms/100 ms·结束 1 s、提交命令 Byte2..Byte6 布局逐项对照上位机代码与 `fan_controller.c`/`bms_fan.c` 确认（Byte2/Byte3 为两个上限、Byte4/Byte5 固定 35/70、Byte6 为 `0xA5`）。本机 257 项测试通过（1 项平台跳过）；`build_macos.sh` 完成测试、PyInstaller、冻结包自检、最低系统版本核对与 ad-hoc 签名并产出 v0.9.7 DMG，冻结程序 `--packaging-smoke-test` 退出码 0 | 本机未连接 PCAN-USB；Windows 安装版的遥测模拟器入口与三种输出、实体 CAN 标定收发、目标机升级与回滚、macOS DMG 覆盖安装仍待验收 |
| 2026-09-14 | 遥测模拟器进 Windows 发布包并清理打包规格：把“遥测模拟器可用”和“实体 CAN 调试模拟通道可用”拆成两个判定，前者两个平台都随包，后者仍只给源码运行与 macOS 过渡版；`can_host.spec` 的 `hiddenimports` 增加 `canhost.telemetry.simulator` / `canhost.bms.simulator` / `serial` / `serial.tools.list_ports_windows`，`excludes` 里失效的 `canhost.simulator` 换成真实存在的 `canhost.vehicle.simulator`；Windows 构建补上冻结包自检，自检按平台分别核对（两平台都要遥测模拟器与完整 TelemetryFrame，macOS 还要能开调试模拟通道，Windows 必须拒绝 `mode=simulation`）。本机用改后的规格跑 PyInstaller 分析（输出到临时目录，不动 `dist/`）：整车模拟器不在包内，BMS 模拟器、遥测模拟器、pyserial 及其 Windows 后端都在；257 项测试通过（1 项平台跳过，新增 `Tests/test_packaging_spec.py` 3 项与冻结 Windows 分支 2 项）；源码级 `--packaging-smoke-test` 在 macOS 分支全流程通过（含 pyserial、帧生成与模拟通道数据） | Windows 分支的自检只能在 Windows 构建上跑（本机把 `sys.platform` 打桩会被 Python 自身 ssl 与 pyserial 的平台分支挡住，无法真实执行）；`.venv-canhost` 未安装 pyserial，本机源码运行看不到串口设备列表；Windows 安装版页面入口与三种输出未实机核对 |
| 2026-09-14 | 修正表头与读数错列及分组标题缩进：ECU 四轮状态表读数改与表头同边左对齐（此前表头左对齐、读数右对齐，同一列内相差约 170px），删除从未被标记使用的 `.veh-ecu-head .num`；六个模组表“状态”表头改为与该列贴右端的读数同边；`.panel-body` 内的分组标题去掉叠加的 12px 缩进（此前标题文字比字段标签右移 38px，涉风扇页 6 处），标题下边框与首行内容之间留 `--sp-2`。252 项测试通过（1 项平台跳过）；用 `scripts/ui_preview.py` 的 live 与 calib 快照在 2400/1600/1460px 下逐块量测：表头与读数左边界差 0px、分组标题与字段标签左边界差 0px、分割线到首行 8.8px、标题/字段网格/进度条/记录表共用 256.2–1416.2 内容边界，无横向越界、无脚本错误。另按“双重缩进”与“贴线元素”两类特征扫描 11 个页面，未再发现同类问题 | 未在 Windows PyWebView 的 100%/125%/150% 缩放下复核；未连接实体 PCAN，真实长数值（三位小数温度、四位转速）下的列宽与换行未复核 |
| 2026-09-14 | 发布 v0.9.6：Windows 与 macOS 构建、GitHub Release、CNB 同步及匿名下载校验全部成功，两端均有 Windows ZIP/校验、Setup、macOS DMG/校验 5 个附件（GitHub 与 CNB 的字节数一致）；发布说明由 CHANGELOG 的 v0.9.6 小节生成，共 15 条。发布链路首次在 Windows CI 上暴露 `scripts/set_version.py` / `scripts/release_notes.py` 打印中文时的 UnicodeEncodeError（英文 Windows 与 CI 的控制台为 cp1252）：两个脚本改为探测当前编码、装不下中文才切 UTF-8，测试按 UTF-8 解码子进程输出；修复随本版本一并发布，因只影响构建与测试工具，未列入随包更新说明 | 本机未连接 PCAN-USB；实体 CAN 收发、Windows PyWebView 与软件内升级仍待目标环境验收 |
| 2026-09-14 | 界面改动复核与失效样式清理：按“选择器里只要有一个类名在前端任何文件里都不存在”扫出 29 处失效规则（`connect-button`、`hv-link`、`tool-connection-*`、`span-8` 等旧标记），删除 24 条规则、改写 5 条选择器；为证明只删了不可能命中的部分，清理前后各自记录 11 个页面、约 5000 个元素的逐元素计算样式快照（字号/颜色/背景/内外边距/边框/圆角/网格/宽高/对齐等 26 项），结果逐页逐元素完全一致。另修掉两处删除规则后残留的空 `@media` 块与拆成两行的 `background` 声明 | HTML 校验只覆盖标签闭合与失效选择器两类问题，未做逐条规则的语义审查 |
| 2026-09-14 | 整车风扇页排版返工：参数一律进 `.field-grid`（列数显式声明、同一行等宽），动作一律另起一行右对齐，同一行字段宽度由 43–178px 收敛到双列 273px / 三列 178px / 四列 280px，修掉“35”显示成“3”、“20%/s”显示成“%/s”、“固定保底”显示成“固定保”的裁切；读数尾巴（数据年龄、目标占空比、转速结论）统一贴右端、温度来源标记合并到一个容器；遥测横条按三组子标题分行，故障位 8 项改 4 列 × 2 行，列数不再出现露底色的空单元；修正过期态失效（此前 `data-stale` 加在单元格内部的 `<b>` 上，风扇页过期配色实际未生效，现在标记整个读数单元）；写入锁定改用与按钮一致的 `mode === "pcan"` 判定，模拟连接下显示提示条并锁住按钮；帧号从剩余提示文字中移除。251 项测试通过（1 项平台跳过）；新增 `scripts/ui_preview.py`，在浏览器里用模拟快照覆盖 live / stale / calib / sim 四种状态，在 1460×920、1680×1000 与 1120×720 下逐块测量字段与读数单元的坐标，无横向越界、无裁切、无脚本错误 | 未在 Windows PyWebView 的 100%/125%/150% 缩放下复核；未连接实体 PCAN，真实数据下的数值宽度和长文本换行未复核；风扇页主区滚动高度从 1463px 增至 1945px（1680×1000；字段标签与动作各占一行换来可读的字段宽度），未在更矮的窗口上复核 |
| 2026-09-14 | 界面统一第二轮（几何与读数原语）：圆角收敛为三档（结构 0、控件与标记 2px、状态点圆形）、字号 6 档、间距 5 档，实测删除 9 种圆角取值、357 处写死字号、579 处写死间距、12 处装饰渐变与全部结构性阴影，卡片头统一 40px；只读读数统一为 `.readout-grid` 单元网格（1px 缝隙、无独立底色边框圆角），BMS 运行总览、整车总览、风扇遥测共用，并删掉逐格描边与 `nth-child` 例外（此前多处叠成 2px 双线）；整车总览改为两个显式列栈，右列 404px 空洞消除、页面 1105→802px；风扇页改为三带结构（遥测横条 768→243px、整行两档功率标定前置、按对象分两列），页面 1999→1463px（该轮把参数与动作并排，字段宽度问题见同日上一条）；折叠箭头、版本对比箭头、弹窗关闭叉与下拉指示改为 1px 折线图形，帧号与机制说明移入悬浮提示；清理 119 条失效样式规则并修正风扇页一处多余闭合标签（该标签曾把 CAN 监视器与遥测故障移出 `main`）。252 项测试通过（1 项平台跳过）；用 Playwright 在 1680×1000 下逐页测量：11 个页面的圆角取值只剩 2px 与状态点、无横向越界、无裁切与行高压缩 | 未在 Windows PyWebView 的 100%/125%/150% 缩放下复核；未连接实体 PCAN，有数据状态下的数值溢出、过期态配色和长数值换行未复核 |
| 2026-09-13 | 界面排版与层级重整：在 styles.css 顶部建立唯一的字号（6 档）、间距（5 档）和圆角（3 档）令牌，逐页把散落的裸值替换为令牌，面板去掉重复内高光、面板内嵌套卡片退化为分隔线；读数与单位改为同一基线内联显示；整车总览改为两列显式分栏并按节点归位；整车风扇页重整为两列加整行标定台、电池箱风扇与标定会话拆卡；常驻协议字符串与固件细节移入悬浮提示并精简状态文字；清除 index.html 内联样式；台架与遥测模拟器页改为并排。252 项测试通过（1 项平台跳过），全部 JavaScript 语法与 Python 编译检查通过；用 Playwright 打开真实页面在 1600×1000 与 1280×800 下逐页截图对比，总览页已单屏装下，风扇页左右两栏高度由 1631/1240 收敛到 1310/1562 | 未在 Windows PyWebView 的 100%/125%/150% 缩放下复核；未用实体 F405、PDM 和 FanController 制造间歇丢帧后复核数据失效与旧值显示 |
| 2026-09-13 | 更新说明链路补齐：升级后显示“更新完成”弹窗（更新前后版本、日期、本次条目、发布页与历史入口），软件更新弹窗改为逐条说明并新增版本历史；说明统一由 `CHANGELOG.md` 经 `scripts/set_version.py`（写入随包说明与历史）和 `scripts/release_notes.py`（生成 Release 正文）产出，两个平台的构建脚本和 `release.yml` 自动调用；新增 `Tests/test_release_notes.py` 与 `Tests/test_update_ui.py`，252 项测试通过（1 项平台跳过），Python/JavaScript 编译检查与发布自检通过；用 Playwright 打开真实页面截图核对三个弹窗的暗色排版与超长条目换行 | 未在 Windows PyWebView 实机核对弹窗；未用真实 vX.Y.Z 标签跑一次完整发布验证 Release 正文与弹窗条目一致 |
| 2026-09-13 | 发布 v0.9.5：Windows 与 macOS 构建、GitHub Release、CNB 同步及匿名下载校验全部成功，两端均有 Windows ZIP/校验/Setup、macOS DMG/校验 5 个附件；将发布首轮暴露的 `.bmslog` 模拟录制固定延时测试改为等待可观测帧数，避免 CI 调度抖动 | 本机未连接 PCAN-USB 或 USB-RS485；实体 CAN 收发、串口目标设备、Broker 认证、Windows PyWebView 和软件内升级仍待目标环境验收 |
| 2026-09-13 | 将两处内容一致的 `local_sim2.py` 整合为工程工具“遥测数据模拟”，支持 MQTT、串口、PCAN 任意组合；源代码 PyWebView 用 `/dev/ttys013` 环回连续运行 41 秒、发送 398 帧并正常停止；MQTT TLS 复用随包 CA 并等待 Broker 确认；225 项测试通过（1 项平台跳过），Python/JavaScript/差异检查通过；v0.9.5 Apple Silicon DMG 构建、冻结包自检、最低系统版本、签名、校验文件及本机 `libPCBUSB` 加载通过 | 本机未连接 PCAN-USB 或 USB-RS485，实体 CAN 收发、串口目标设备、Broker 认证与组合输出仍待硬件/现网验收 |
| 2026-09-13 | 将监视值留存与控制新鲜度分离：电芯、整车、PDM、两套风扇及快捷栏在过期后保留最后有效值并显示年龄/“旧”，断线、离线、无效值和活动故障不冒充当前状态；218 项测试通过（1 项平台跳过），Python 编译、全量 JavaScript 语法、差异检查和 macOS PyWebView 的整车/风扇模拟页面通过 | 实体 F405、PDM、FanController 的间歇丢帧、从控 70 ms/帧调试模式与 Windows PyWebView 视觉核对 |
| 2026-09-12 | 发布 v0.9.4：Windows 与 macOS 构建、GitHub Release、CNB 同步全部成功，两端均生成 5 个正式附件；版本包含 v0.9.3 的更新助手修复及普通弹窗遮罩关闭交互 | v0.9.1/v0.9.2 仍需手动运行 v0.9.4 Setup；Windows PyWebView 实测弹窗交互和后续软件内升级 |
| 2026-09-12 | 普通连接设置、MQTT 和软件更新弹窗增加遮罩空白处关闭；高风险命令确认弹窗继续要求明确取消或确认，避免误触关闭 | Windows PyWebView 实测点击遮罩、弹窗内容区、取消、关闭按钮和 Esc 的行为 |
| 2026-09-12 | 发布 v0.9.3：Windows 与 macOS 构建、GitHub Release、CNB 同步全部成功；GitHub 和 CNB 均有 Windows ZIP/校验/Setup、macOS DMG/校验 5 个附件，CNB 更新频道与 16 个现存附件匿名下载校验全部通过 | v0.9.1/v0.9.2 自带旧助手，需手动安装一次 v0.9.3；随后用下一版本验证修复后的软件内更新链路 |
| 2026-09-12 | 定位 v0.9.1→v0.9.2 本机更新失败：安装助手继承快捷方式的安装目录工作路径，导致其自身阻止目录重命名；旧版自动恢复并重启。修复为从更新临时目录的父目录启动助手，并为目录重命名增加有限重试 | 手动安装 v0.9.3 后，用后续版本核对目录替换、新版健康确认、卸载入口与临时目录清理 |
| 2026-09-12 | v0.9.2 双平台发布成功：GitHub Release 与 CNB 均包含 Windows ZIP、ZIP 校验、Setup、macOS arm64 DMG、DMG 校验共 5 个附件；GitHub Actions 的 Windows 构建、macOS arm64 构建、发布和 CNB 同步全部通过 | 干净 Windows/macOS 目标机安装、软件升级回滚、Gatekeeper 和实体 PCAN |
| 2026-09-12 | 发布 v0.9.1 并用真实发布产物完成本机隔离升级演练：用已发布 v0.9.1 ZIP 搭出"已安装的 v0.9.0 客户端"（跑 0.9.1 代码但上报 0.9.0），使其对公开 CNB 频道检查更新。实测命中 `source=cnb`、tag `v0.9.1`，自动下载并校验 ZIP、`launch_installer` 交接真实嵌入助手脚本、助手等待旧进程退出后备份旧目录并换入新目录、启动发布版 `BITFSAE_CAN_Host.exe`（PID 36764）。新版完成初始化并回写健康信号后，助手核对进程号与版本号，日志记录 `frontend healthy pid=36764 version=0.9.1` 与 `install success pid=36764`。同时直接驱动嵌入助手脚本在 `%TEMP%` 跑失败链路，确认旧目录恢复、旧版本重启、临时目录清理，退出码 23。另独立复核：干净 venv 187 项单测全绿、`node --check` 9 个脚本通过、Windows PowerShell 5.1 实际解析嵌入助手脚本、PyInstaller one-folder 构建成功。GitHub Release 三个附件已发布；`release.yml` 的 CNB 同步步骤因步骤级 `env` 覆盖作业级 `env`、缺少 `GH_TOKEN` 而失败（PR #6 修复合并），v0.9.1 镜像由本机 `cnb_publish.py sync` 补齐并匿名校验通过：三个附件可匿名下载，`.sha256` 内容与 GitHub digest `95b3fe5e…` 完全一致，更新频道已指向 v0.9.1 | 未在真实 Windows 10/11 上用 v0.9.0 安装版走一次软件内更新（安装目录写权限、快捷方式、卸载入口与整机退出看门狗）；CI 内 CNB 同步步骤待下一次标签发布验证 |
| 2026-09-12 | 软件更新入口改为下载与重启两步；侧栏显示下载图标，下载中显示百分比、已下载大小和速度，校验完成后由用户点击“重启更新”；207 项测试通过（1 项平台跳过），Python 与 JavaScript 语法检查通过 | Windows 发布版真实升级/回滚、CNB 正式发布和目标机验证 |
| 2026-09-12 | 审查问题修复：电池箱提交按钮改按最后已知完成状态使能、步骤采样加一轮复采兜底、中止文案补手动恢复路径、清理风扇页冗余禁用赋值；206 项测试通过（1 项平台跳过），Python 编译与 JS 语法检查通过 | 实体 PCAN 下提交按钮最后已知状态判定、最小 hold 复采时序和真实负载曲线 |
| 2026-09-12 | 两套风扇标定完成安全、时序和并发审计；205 项测试通过（1 项平台跳过），Python 编译、JavaScript 语法及差异检查通过 | 实体 PCAN 下的 `0x5A9/0x5AD` 回读时序、租约归零、真实负载曲线和断电保存 |
| 2026-09-12 | 合并远端 v0.9.1 与本地 macOS 发布链路；192 项测试通过（1 项平台跳过），Python/JavaScript/Bash、YAML 和差异检查通过；DMG 重建、签名、SHA256、挂载自检及 `libPCBUSB` 加载通过 | 双平台 CI、CNB 五附件、实体 PCAN、干净目标机 |
| 2026-09-12 | Windows 更新助手在隔离目录完成正常升级和健康不匹配回滚；187 项测试及 Windows PyInstaller 构建通过 | 真实旧版到新版升级、目标机权限和卸载入口 |
| 2026-09-11 | CNB 已镜像 v0.9.0 Windows 附件；匿名下载、更新器命中、定时/API 补镜像和 182 项测试通过 | 首次双平台正式发布和 Windows 发布版实机更新 |
| 2026-09-04 | v0.9.0 CAN 监视器与 Windows 发布链路完成 157 项测试和 CI 构建 | 实体双 PCAN、长时间记录、非默认总线发送 |
